## 🔹 **1. DML (Data Manipulation Language)** — язык манипуляции данными

Используется для работы **с содержимым таблиц** (данными).

- **INSERT** — добавление строк.
    
    `INSERT INTO users (id, name, age) VALUES (1, 'Alex', 25);`
    
- **UPDATE** — изменение данных.
    
    `UPDATE users SET age = 26 WHERE id = 1;`
    
- **DELETE** — удаление строк.
    
    `DELETE FROM users WHERE id = 1;`
    
- **SELECT** — выборка данных (самая часто используемая команда).
    
    `SELECT name, age FROM users WHERE age > 20;`
    

👉 Все DML-операции **можно откатить (ROLLBACK)**, если они выполняются в транзакции.

---

## 🔹 **2. DDL (Data Definition Language)** — язык определения данных

Работает **со структурой базы данных** (схемой: таблицы, индексы, представления и т.д.).

- **CREATE** — создание объектов.
    
    `CREATE TABLE users (     id INT PRIMARY KEY,     name VARCHAR(100),     age INT );`
    
- **ALTER** — изменение структуры.
    
    `ALTER TABLE users ADD email VARCHAR(255);`
    
- **DROP** — удаление объектов.
    
    `DROP TABLE users;`
    

👉 DDL-команды обычно **автоматически фиксируются (auto-commit)** и **откатить их нельзя** (в большинстве СУБД).

---

## 🔹 **3. DCL (Data Control Language)** — управление доступом

Отвечает за **права пользователей**.

- **GRANT** — выдача прав.
    
    `GRANT SELECT, INSERT ON users TO 'vasya';`
    
- **REVOKE** — отзыв прав.
    
    `REVOKE INSERT ON users FROM 'vasya';`
    

👉 Используется в администрировании: можно ограничить доступ на чтение/запись к таблицам.

---

## 🔹 **4. TCL (Transaction Control Language)** — управление транзакциями

Позволяет **управлять целостностью данных** при изменениях.

- **COMMIT** — фиксирует транзакцию.
    
    `COMMIT;`
    
- **ROLLBACK** — откат транзакции.
    
    `ROLLBACK;`
    
- **SAVEPOINT** — создание точки сохранения внутри транзакции.
    
    `SAVEPOINT sp1; 
    UPDATE users SET age = 30 WHERE id = 2; 
    ROLLBACK TO sp1;`
    
- **SET TRANSACTION** — задание свойств транзакции (уровень изоляции, read-only и др.).
    
    `SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;`
    

👉 TCL нужен, чтобы **гарантировать согласованность данных** (ACID-свойства).

---

## ✨ Итоговая схема:

- **DML** → Работаем с данными (SELECT, INSERT, UPDATE, DELETE)
    
- **DDL** → Работаем со структурой БД (CREATE, ALTER, DROP)
    
- **DCL** → Работаем с доступами (GRANT, REVOKE)
    
- **TCL** → Работаем с транзакциями (COMMIT, ROLLBACK, SAVEPOINT)

## 🔹 Что такое транзакция

Транзакция — это **логическая группа операций**, которые должны выполниться "всё или ничего" (атомарность).

Она подчиняется свойствам **ACID**:

- **A (Atomicity)** — либо все изменения применяются, либо ни одно.
    
- **C (Consistency)** — после транзакции данные остаются согласованными.
    
- **I (Isolation)** — параллельные транзакции не мешают друг другу.
    
- **D (Durability)** — результат транзакции сохраняется навсегда (даже при сбое).
## 🔹 Два вида журналов (логи)

Под капотом транзакции работают благодаря **журналам изменений**:

1. **Undo log (журнал отката)**
    
    - Хранит _старые значения_ данных (до изменения).
        
    - Используется для **ROLLBACK** (отката транзакции).
        
    - Пример: если мы обновили `balance = 100 → 50`, в undo log запишется `balance = 100`.
        
2. **Redo log (журнал повторного применения)**
    
    - Хранит _новые значения_ данных (после изменения).
        
    - Используется для восстановления БД при сбое (например, отключилось электричество).
        
    - Пример: если `balance = 100 → 50`, то в redo log запишется `balance = 50`.
        

⚡ Важно: **запись всегда сначала идёт в лог, потом в данные!** Это называется **Write-Ahead Logging (WAL)**.

---

## 🔹 Как работает COMMIT

Представь, что мы делаем `UPDATE accounts SET balance = balance - 50 WHERE id = 1;`

1. Клиент послал команду `UPDATE`.
    
2. СУБД пишет:
    
    - в **undo log**: старое значение `balance = 100`,
        
    - в **redo log**: новое значение `balance = 50`.
        
3. Данные изменяются **в буфере** (не сразу в файле на диске).
    
4. Когда вызывается `COMMIT`:
    
    - СУБД гарантирует, что **redo log записан на диск**.
        
    - Только после этого транзакция считается успешной.
        

👉 Таким образом, даже если сервер упадёт, при старте СУБД прочитает redo log и восстановит состояние.

---

## 🔹 Как работает ROLLBACK

Если вместо `COMMIT` сделать `ROLLBACK`:

1. СУБД берёт данные из **undo log**.
    
2. Восстанавливает старое значение (`balance = 100`).
    
3. Транзакция считается отменённой.
    

---

## 🔹 SAVEPOINT

- Это "точка сохранения" внутри транзакции.
    
- Если сделать `SAVEPOINT A`, а потом `ROLLBACK TO A`, СУБД откатит только изменения после этой точки, используя **undo log**.
    

---

## 🔹 Восстановление после сбоя

Представим ситуацию:

- Мы обновили запись (`balance = 100 → 50`).
    
- В **redo log** уже записано новое значение.
    
- Но сервер упал до того, как данные записались в таблицу.
    

👉 При перезапуске СУБД:

1. Читает **redo log**.
    
2. Повторно применяет изменения.
    
3. Таким образом, **COMMIT не теряется**, даже если таблица ещё не успела обновиться.
    

Если же был только **undo log** (без redo), то восстановить "успешный COMMIT" нельзя было бы.

---

## 🔹 Итог

- **Undo log** нужен для ROLLBACK и изоляции транзакций.
    
- **Redo log** нужен для восстановления после сбоя и гарантии Durability.
    
- `COMMIT` = фиксация изменений в redo log.
    
- `ROLLBACK` = возврат старых данных из undo log.
    
- `SAVEPOINT` = "частичный откат" внутри транзакции.