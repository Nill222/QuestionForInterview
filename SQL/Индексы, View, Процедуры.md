## 🔹 Индексы

### Как ускоряют поиск

- Без индекса СУБД ищет данные **полным сканированием таблицы (Full Table Scan)** — построчно проверяет каждую запись.
    
- С индексом поиск идёт по **структуре данных** (чаще всего **B-дерево** или его вариация B+Tree):
    
    - Ключи отсортированы.
        
    - Алгоритм поиска — **log(n)** вместо **O(n)**.
        
    - При `WHERE`, `JOIN`, `ORDER BY` СУБД может использовать индекс.
        

📌 Пример:

`CREATE INDEX idx_user_name ON users(name);`

Теперь `SELECT * FROM users WHERE name = 'Alex'` будет работать в разы быстрее.

---

### Когда индексы могут не использоваться

- Если запрос возвращает **слишком много строк** (часто >30% таблицы) → СУБД решает, что проще пройтись полностью.
    
- Функции над столбцом ломают использование индекса:
    
    `SELECT * FROM users WHERE LOWER(name) = 'alex'; -- индекс не сработает`
    
- При неравенстве (`<>`, `NOT IN`, `NOT LIKE '%abc'`).
    
- Если индекс не покрывает условия (например, фильтр по столбцу без индекса).
    

---

### Какие бывают индексы

1. **Clustered index (кластерный)**
    
    - Таблица физически хранится в порядке ключа индекса.
        
    - В каждой таблице может быть **только один** кластерный индекс (обычно на `PRIMARY KEY`).
        
    - Пример: в SQL Server или MySQL InnoDB Primary Key автоматически кластерный.
        
2. **Non-clustered index (некластерный)**
    
    - Отдельная структура с ключами и ссылками на реальные строки.
        
    - Может быть **несколько**.
        
    - Работает как "карта" к данным.
        
3. Дополнительно:
    
    - **Unique index** — запрещает дубликаты.
        
    - **Composite index** — на несколько колонок.
        
    - **Full-text index** — для поиска по тексту.
        
    - **Bitmap index** — эффективен для столбцов с малым числом уникальных значений.
        

---

## 🔹 View (Представления)

### Что это такое

`VIEW` — это **сохранённый SQL-запрос**, который выглядит как таблица, но сам данных не хранит (кроме материализованных).

`CREATE VIEW active_users AS SELECT id, name FROM users WHERE status = 'ACTIVE';`

Теперь можно:

`SELECT * FROM active_users;`

---

### Когда помогает

- Сокращает сложные запросы (инкапсуляция логики).
    
- Удобно для безопасности (дать доступ к view, а не к исходной таблице).
    
- Упрощает интеграцию (API или BI-инструменты работают с "виртуальной таблицей").
    

---

### Отличия между материализованным и обычным

- **Обычный VIEW**
    
    - Хранит только SQL-запрос.
        
    - Каждый вызов → выполняется заново.
        
- **Материализованный VIEW**
    
    - Хранит результат запроса (как кэш).
        
    - Нужно вручную или по расписанию обновлять (`REFRESH MATERIALIZED VIEW`).
        
    - Очень полезно для аналитических запросов на больших данных.
        

---

## 🔹 Процедуры и функции

### Разница между ними

- **Процедура (`PROCEDURE`)**
    
    - Не обязана возвращать значение.
        
    - Может выполнять DML (INSERT, UPDATE, DELETE).
        
    - Обычно используют для бизнес-логики в БД.
        
- **Функция (`FUNCTION`)**
    
    - Всегда возвращает значение (скалярное или табличное).
        
    - Может использоваться прямо в SQL (например, `SELECT my_func(col)`).
        
    - Ограничена в действиях (не всегда разрешён `INSERT`/`UPDATE`).
        

---

### IN, OUT и INOUT параметры

- **IN** — передаём значение в процедуру (по умолчанию).
    
- **OUT** — возвращаем результат через параметр.
    
- **INOUT** — и вход, и выход одновременно.
    

📌 Пример:

`CREATE PROCEDURE get_user_count(IN status_val VARCHAR(20), OUT cnt INT) BEGIN   SELECT COUNT(*) INTO cnt FROM users WHERE status = status_val; END;`

---

### Когда реально полезны

- Процедуры:
    
    - Автоматизация бизнес-логики (например, начисление зарплаты).
        
    - Массовые обновления в транзакциях.
        
- Функции:
    
    - Повторное использование вычислений (`age(birthdate)`).
        
    - Сложные условия фильтрации (`WHERE calc_tax(salary) > 1000`).