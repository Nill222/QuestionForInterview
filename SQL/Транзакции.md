
---

# 🔹 **Транзакции в СУБД**

## 1. ❓ Что такое транзакция и зачем она нужна

**Транзакция** — это логическая единица работы с базой данных, состоящая из одного или нескольких SQL-операторов, которые выполняются как **одно целое**.

- Либо **все изменения фиксируются (COMMIT)**,
    
- Либо **откатываются (ROLLBACK)**, если произошла ошибка.
    

👉 Зачем:

- Обеспечивает **целостность данных**.
    
- Позволяет **группировать операции** (например, перевод денег: списание + зачисление).
    
- Гарантирует, что база не останется в «полусломанном» состоянии.
    

---

## 2. 🧾 ACID-принципы

ACID — это набор свойств, которые должны обеспечивать транзакции.

1. **Atomicity (Атомарность)**
    
    - Транзакция либо выполняется полностью, либо не выполняется вообще.
        
    - Обеспечивается через **undo log** → если сбой, система откатывает изменения.
        
2. **Consistency (Согласованность)**
    
    - После завершения транзакции база должна оставаться в согласованном состоянии (не нарушаются ограничения `PRIMARY KEY`, `FOREIGN KEY`, `CHECK`).
        
3. **Isolation (Изоляция)**
    
    - Одновременные транзакции не должны мешать друг другу.
        
    - Обеспечивается **механизмами блокировок (locks)** или **многоверсионностью (MVCC)**.
        
4. **Durability (Надёжность)**
    
    - Если транзакция закоммичена, изменения не потеряются даже при сбое.
        
    - Гарантируется с помощью **redo log** (журнал перезаписи).
        

---

## 3. 🧩 Уровни изоляции (Isolation Levels)

SQL стандарт (ANSI/ISO) определяет 4 уровня изоляции:

1. **READ UNCOMMITTED (чтение неподтверждённых данных)**
    
    - Можно читать даже те данные, которые другая транзакция ещё не закоммитила.
        
    - ⚠ Проблемы: _dirty read, non-repeatable read, phantom read_.
        
    - На практике почти не используется.
        
2. **READ COMMITTED (чтение подтверждённых данных)**
    
    - Читаем только закоммиченные изменения.
        
    - ✅ Убирает dirty reads.
        
    - ⚠ Но остаются _non-repeatable reads_ и _phantom reads_.
        
    - Часто используется (Oracle по умолчанию).
        
3. **REPEATABLE READ (повторяемость чтения)**
    
    - Если в транзакции мы читаем строку, она не изменится в течение транзакции.
        
    - ✅ Убирает dirty read и non-repeatable read.
        
    - ⚠ Но возможны phantom reads.
        
    - Используется, например, в MySQL (InnoDB по умолчанию).
        
4. **SERIALIZABLE (сериализуемость)**
    
    - Максимальная изоляция — транзакции выполняются как будто последовательно.
        
    - ✅ Убирает все проблемы.
        
    - ⚠ Самый медленный режим (много блокировок).
        

---

## 4. ⚠ Типичные проблемы конкурентности

1. **Dirty Read (грязное чтение)**
    
    - Одна транзакция читает данные, которые другая изменила, но ещё не закоммитила.
        
    - Если будет `ROLLBACK`, данные окажутся «мусорными».
        
    
    🔹 Пример:
    
    - T1: `UPDATE balance SET amount=0 WHERE id=1;` (ещё не commit)
        
    - T2: `SELECT amount FROM balance WHERE id=1;` → видит `0`, хотя потом T1 откатится.
        

---

2. **Non-Repeatable Read (неповторяемое чтение)**
    
    - В рамках одной транзакции повторное чтение строки может дать разные результаты.
        
    
    🔹 Пример:
    
    - T1: `SELECT balance FROM account WHERE id=1;` → 100
        
    - T2: `UPDATE account SET balance=200 WHERE id=1; COMMIT;`
        
    - T1: снова `SELECT balance FROM account WHERE id=1;` → уже 200
        

---

3. **Phantom Read (фантомное чтение)**
    
    - При повторном запросе могут появляться/исчезать новые строки.
        
    
    🔹 Пример:
    
    - T1: `SELECT * FROM orders WHERE amount > 100;` (10 строк)
        
    - T2: `INSERT INTO orders VALUES (11, 150); COMMIT;`
        
    - T1: снова `SELECT ...` → уже 11 строк.
        

---

# 📌 Итог

- **Транзакции** нужны для целостности данных.
    
- **ACID** описывает их базовые свойства.
    
- **Уровни изоляции** регулируют баланс между скоростью и безопасностью.
    
- Проблемы (dirty/non-repeatable/phantom reads) решаются выбором правильного isolation level.