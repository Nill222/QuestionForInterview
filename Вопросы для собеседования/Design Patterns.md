## **1. Зачем нужны паттерны? Привести примеры из проекта**

### **Зачем нужны паттерны проектирования**

📌 **Паттерны** — это проверенные временем решения **типовых архитектурных проблем**.

Они нужны для:

- повторного использования лучших практик
    
- улучшения читаемости кода
    
- упрощения поддержки и расширения
    
- общего языка между разработчиками
    
- уменьшения связности (loose coupling)
    

---

### **Пример из проекта**

(универсальный, можно использовать почти везде)

#### **Factory**

> Использовали Factory для создания клиентов разных типов (REST, SOAP), чтобы не привязывать бизнес-логику к конкретной реализации.

#### **Builder**

> Применяли Builder для создания сложных DTO с большим количеством optional-полей.

#### **Strategy**

> Использовали Strategy для выбора алгоритма расчёта скидки в зависимости от типа пользователя.

---

### **Фраза для интервью**

> Паттерны позволяют решать типовые архитектурные задачи единообразно, повышают читаемость и упрощают поддержку кода.

---

## **2. Какие паттерны вы знаете и как их применяет Java SE**

### **Основные группы паттернов**

- **Порождающие**
    
- **Структурные**
    
- **Поведенческие**
    

---

### **Примеры из Java SE**

|Паттерн|Где используется|
|---|---|
|Singleton|`Runtime`, `Desktop`|
|Factory Method|`Calendar.getInstance()`|
|Abstract Factory|`DocumentBuilderFactory`|
|Builder|`StringBuilder`, `Locale.Builder`|
|Prototype|`Object.clone()`|
|Adapter|`Arrays.asList()`|
|Decorator|`InputStream`|
|Observer|`PropertyChangeListener`|
|Strategy|`Comparator`|
|Template Method|`HttpServlet`|

---

### **Интервью-фраза**

> Java SE активно использует паттерны, например Singleton в Runtime, Factory Method в Calendar, Decorator в InputStream.
Порождающие паттерны отвечают за создание объектов, структурные — за композицию и расширение, поведенческие — за взаимодействие и распределение логики. Java SE активно использует все эти паттерны, например Singleton в Runtime, Decorator в InputStream и Strategy в Comparator.
---

## **3. Factory Method и Builder**

---

### **Factory Method**

📌 **Задача**

- Инкапсулировать создание объекта
    
- Убрать `new` из клиентского кода
    

`public interface Transport {     void move(); }  public class Car implements Transport { }  public class TransportFactory {     public static Transport create(String type) {         if ("car".equals(type)) return new Car();         return null;     } }`

📌 **Когда использовать**

- Есть несколько реализаций
    
- Не хотим зависеть от конкретных классов
    

---

### **Builder**

📌 **Задача**

- Создание сложных объектов
    
- Много optional-полей
    

`User user = User.builder()     .name("Alex")     .age(30)     .email("a@mail.com")     .build();`

📌 **Плюсы**

- Читаемость
    
- Иммутабельность
    
- Нет телескопических конструкторов
    

---

### **Ключевое отличие**

|Factory|Builder|
|---|---|
|Создаёт разные типы|Создаёт один сложный объект|
|Обычно один метод|Пошаговое создание|

---

## **4. Singleton (class Runtime)**

### **Что такое Singleton**

📌 Гарантирует **один экземпляр класса** на JVM.

---

### **Пример из Java**

`Runtime runtime = Runtime.getRuntime();`

- Конструктор `private`
    
- Единственный экземпляр создаётся внутри класса
    

---

### **Зачем Runtime Singleton**

- Управляет ресурсами JVM
    
- Нет смысла создавать несколько экземпляров
    

---

### **Интервью-фраза**

> Singleton используется, когда необходим единственный глобальный объект, например Runtime в Java.

---

## **5. Как сделать, чтобы в Singleton не работала двойная блокировка?**

❗ Вопрос с подвохом.

### **Проблема double-checked locking**

Без `volatile` объект может быть **частично инициализирован**.

---

### **Правильные способы**

#### ✅ **Enum Singleton (лучший)**

`public enum Singleton {     INSTANCE; }`

- Потокобезопасен
    
- Защищён от сериализации и reflection
    

---

#### ✅ **Initialization-on-demand holder**
``` Java

`public class Singleton {     
private Singleton() {}      
private static class Holder {         
private static final Singleton INSTANCE = new Singleton();     }      
public static Singleton getInstance() {         
return Holder.INSTANCE;     } }
```
---

### **Ответ для интервью**

> Чтобы избежать проблем двойной блокировки, лучше использовать enum или holder-паттерн, а не double-checked locking.

---

## **6. Как у Singleton создать второй объект? И как этому помешать**

### **Способы взлома Singleton**

---

#### ❌ **Reflection**

`Constructor<Singleton> c = Singleton.class.getDeclaredConstructor(); c.setAccessible(true); Singleton s2 = c.newInstance();`

🔐 **Защита**

`private Singleton() {     if (instance != null) {         throw new RuntimeException();     } }`

---

#### ❌ **Serialization**

`ObjectInputStream.readObject();`

🔐 **Защита**

`protected Object readResolve() {     return INSTANCE; }`

---

#### ❌ **Clone**

`clone()`

🔐 **Защита**

`@Override protected Object clone() throws CloneNotSupportedException {     throw new CloneNotSupportedException(); }`

---

### **Почему enum лучший**

- JVM гарантирует один экземпляр
    
- Защищён от всех способов взлома
    

---

### **Идеальный ответ**

> Singleton можно взломать через reflection, serialization или clone. Лучший способ защиты — использование enum.

## **7. Prototype**

### **Суть паттерна**

📌 **Prototype** позволяет создавать новые объекты **путём клонирования существующего**, а не через `new`.

---

### **Когда нужен**

- Создание объекта дорогое
    
- Нужно быстро получить копию
    
- Конфигурация объекта уже есть
    

---

### **Пример**
```Java

`public class User implements Cloneable {     
private String name;      
@Override     
protected User clone() throws CloneNotSupportedException {         
return (User) super.clone();     } }`
```
---

### **Где в Java**

- `Object.clone()`
    
- `ArrayList.clone()`
    
- `HashMap.clone()`
    

---

### **Подводные камни**

❌ Поверхностное копирование  
❌ Нарушение инкапсуляции  
❌ Cloneable — marker interface

---

### **Интервью-фраза**

> Prototype позволяет создавать объект путём клонирования, а в Java реализуется через clone().

---

## **8. Command**

### **Суть**

📌 Инкапсулирует запрос как объект.

---

### **Когда использовать**

- Очереди команд
    
- Undo / Redo
    
- Логирование действий
    

---

### **Пример**
```Java


`interface Command {     
void execute(); }  
class SaveCommand implements Command {     p
ublic void execute() {         
System.out.println("Saving...");     } }`
```
---

### **Где в Java**

- `Runnable`
    
- `Callable`
    
- `ActionListener`
    

---

### **Интервью**

> Command превращает запрос в объект, что позволяет параметризовать клиент кодом команд.

---

## **9. Composite**

### **Суть**

📌 Позволяет работать с одиночными объектами и группами **одинаково**.

---

### **Пример**
```Java

`interface Component {     
void draw(); }  
class Leaf implements Component { }  
class Composite implements Component {     
List<Component> children; }`
```
---

### **Где используется**

- Swing (`JComponent`)
    
- DOM
    
- Файловые системы
    

---

### **Интервью**

> Composite позволяет представлять древовидные структуры и работать с ними единообразно.

---

## **10. Chain of Responsibility**

_(Filter, closing io stream, connection)_

### **Суть**

📌 Запрос проходит по цепочке обработчиков, пока не будет обработан.

---

### **Пример**

`abstract class Handler {     protected Handler next;     abstract void handle(); }`

---

### **Примеры из Java**

- `javax.servlet.Filter`
    
- `try-with-resources` (закрытие ресурсов)
    
- `Logger`
    

---

### **Как закрываются ресурсы**

`try (     InputStream in = ...     Connection con = ... ) { }`

Закрытие идёт **в обратном порядке**.

---

### **Интервью**

> Chain of Responsibility позволяет передавать запрос по цепочке обработчиков без жёсткой привязки.

---

## **11. State (Thread.State)**

### **Суть**

📌 Поведение объекта меняется в зависимости от его состояния.

---

### **Пример**

`Thread.State state = thread.getState();`

Состояния:

- NEW
    
- RUNNABLE
    
- BLOCKED
    
- WAITING
    
- TIMED_WAITING
    
- TERMINATED
    

---

### **Когда использовать**

- Сложная логика состояний
    
- Убирает if-else
    

---

### **Интервью**

> State позволяет изменять поведение объекта при смене состояния без изменения кода клиента.

---

## **12. Iterator**

_(Enumeration, Iterator, ListIterator, ResultSet)_

---

### **Суть**

📌 Предоставляет единый способ обхода коллекций.

---

### **Iterator**

`Iterator<String> it = list.iterator(); while (it.hasNext()) {     it.next(); }`

- Можно удалять
    
- Fail-fast
    

---

### **Enumeration**

- Legacy
    
- Только чтение
    
- Не fail-fast
    

---

### **ListIterator**

- Только для List
    
- Движение в обе стороны
    
- Изменение элементов
    

---

### **ResultSet как Iterator**

`while (rs.next()) { }`

- `next()`
    
- `previous()` (scrollable)
    
- `first()`, `last()`
    

---

### **Интервью-таблица**

|Тип|Назад|Изменение|Fail-fast|
|---|---|---|---|
|Enumeration|❌|❌|❌|
|Iterator|❌|remove|✅|
|ListIterator|✅|add/set|✅|

---

### **Интервью**

> Iterator скрывает внутреннюю структуру коллекции и предоставляет стандартный способ обхода.

## **13. Proxy**

### **Суть паттерна**

📌 **Proxy** — это объект-заместитель, который контролирует доступ к другому объекту.

---

### **Зачем нужен**

- ленивое создание (lazy loading)
    
- безопасность
    
- логирование
    
- транзакции
    

---

### **Типы Proxy**

- **Virtual Proxy**
    
- **Protection Proxy**
    
- **Remote Proxy**
    

---

### **Пример**

`interface Service {     void process(); }  class RealService implements Service {     public void process() { } }  class ServiceProxy implements Service {     private RealService real;     public void process() {         if (real == null) real = new RealService();         real.process();     } }`

---

### **Где в Java / Spring**

- `java.lang.reflect.Proxy`
    
- Spring AOP
    
- Hibernate Lazy Loading
    

---

### **Интервью**

> Proxy позволяет добавить поведение к объекту без изменения его кода, часто используется в Spring AOP.

---

## **14. Observer (Listener-s)**

### **Суть**

📌 Объекты подписываются на события и автоматически уведомляются.

---

### **Пример**

`interface Observer {     void update(); }`

---

### **Java примеры**

- `EventListener`
    
- `ActionListener`
    
- `PropertyChangeListener`
    

---

### **Spring**

- `ApplicationListener`
    
- `@EventListener`
    

---

### **Плюсы / минусы**

✅ Слабая связность  
❌ Возможны утечки памяти

---

### **Интервью**

> Observer реализует механизм подписки и уведомления, в Java часто представлен в виде Listener’ов.

---

## **15. Wrapper (Decorator)**

⚠️ Часто путают с Adapter — **на интервью важно разделять**.

---

### **Суть Decorator**

📌 Динамически добавляет функциональность объекту.

---

### **Пример**

`InputStream in =     new BufferedInputStream(         new FileInputStream("a.txt")     );`

---

### **Где в Java**

- `InputStream`
    
- `OutputStream`
    
- `Collections.synchronizedList()`
    

---

### **Отличие от Adapter**

|Decorator|Adapter|
|---|---|
|Расширяет|Преобразует интерфейс|
|Тот же интерфейс|Другой интерфейс|

---

### **Интервью**

> Wrapper (Decorator) позволяет оборачивать объект, добавляя поведение без изменения исходного класса.

---

## **16. Immutable**

### **Что такое immutable**

📌 Объект, состояние которого нельзя изменить после создания.

---

### **Как сделать immutable**

- `final class`
    
- `private final fields`
    
- нет setters
    
- defensive copy
    

---

### **Пример**

`public final class User {     private final String name; }`

---

### **Плюсы**

- потокобезопасность
    
- простота
    
- подходит для cache
    

---

### **Примеры в Java**

- `String`
    
- `Integer`
    
- `LocalDate`
    

---

### **Интервью**

> Immutable объекты безопасны для многопоточности и проще в использовании.

---

## **17. MVC**

### **Что такое MVC**

📌 Архитектурный паттерн:

- **Model**
    
- **View**
    
- **Controller**
    

---

### **Роли**

- Model — бизнес-логика
    
- View — отображение
    
- Controller — обработка запросов
    

---

### **Spring MVC**

- Controller — `@Controller`
    
- Model — DTO / Entity
    
- View — Thymeleaf / JSP / JSON
    

---

### **Интервью**

> MVC разделяет ответственность между слоями и упрощает поддержку приложения.

---

## **18. DAO vs Repository**

### **DAO**

📌 Паттерн доступа к данным  
Работает с БД напрямую

`UserDao.findById()`

---

### **Repository**

📌 Более высокий уровень  
Работает с **доменной моделью**

`UserRepository.save()`

---

### **Основные отличия**

|DAO|Repository|
|---|---|
|Низкоуровневый|Доменно-ориентированный|
|CRUD + SQL|CRUD + бизнес|
|JDBC|JPA / Spring Data|

---

### **Spring Data**

- Repository — абстракция над DAO
    
- Автогенерация запросов
    

---

### **Интервью**

> DAO работает с источником данных, Repository — с доменной моделью и является более высокоуровневой абстракцией.