## 88. Основные интерфейсы коллекций и их иерархия

- **Корень:** `Iterable` → позволяет проходить элементы через `Iterator`.
    
- **Collection** — базовый интерфейс для всех коллекций (кроме `Map`).
    
    - **List** — упорядоченные коллекции, допускают дубликаты.
        
        - Реализации: `ArrayList`, `LinkedList`, `Vector`, `Stack`.
            
    - **Set** — множества, уникальные элементы.
        
        - Реализации: `HashSet`, `LinkedHashSet`, `TreeSet`.
            
    - **Queue** — очереди, FIFO/LIFO.
        
        - Реализации: `PriorityQueue`, `ArrayDeque`, `LinkedList`.
            
- **Map** — отдельный интерфейс (не наследуется от `Collection`), хранит пары ключ-значение.
    
    - Реализации: `HashMap`, `LinkedHashMap`, `TreeMap`, `Hashtable`, `ConcurrentHashMap`.
        

👉 Особенности:

- `List` — порядок важен, допускает дубликаты.
    
- `Set` — уникальность элементов.
    
- `Queue` — управление порядком обработки.
    
- `Map` — работа с ключами и значениями.
    

## 89. Интерфейс `Map`

- Хранит пары **ключ → значение**.
    
- Не является коллекцией (`Collection`), но часть Java Collections Framework.
    
- Особенности:
    
    - Ключи уникальны.
        
    - Значения могут повторяться.
        
    - Методы: `put()`, `get()`, `containsKey()`, `entrySet()`.
        

## 90. Сравнение `ArrayList` и `LinkedList`

|Характеристика|ArrayList|LinkedList|
|---|---|---|
|Основа|Массив|Двусвязный список|
|Доступ по индексу|O(1)|O(n)|
|Вставка/удаление в середине|O(n)|O(1) (если есть ссылка на узел)|
|Память|Компактнее|Больше (ссылки на соседей)|
|Когда использовать|Частые чтения|Частые вставки/удаления|

## 91. Сравнение `HashMap` и `Hashtable`

|Характеристика|HashMap|Hashtable|
|---|---|---|
|Потокобезопасность|Нет|Да (все методы synchronized)|
|Null|Допускает `null` ключ и значения|Не допускает|
|Производительность|Быстрее|Медленнее|
|Использование|Современный стандарт|Устарел, заменён на `ConcurrentHashMap`|

## 92. Сравнение `HashSet` и `TreeSet`

|Характеристика|HashSet|TreeSet|
|---|---|---|
|Основа|HashMap|TreeMap (красно-чёрное дерево)|
|Порядок|Не гарантируется|Сортированный|
|Скорость|O(1) операции|O(log n) операции|
|Когда использовать|Уникальные элементы без порядка|Уникальные элементы с сортировкой|

## 93. Принцип работы `HashMap` и изменения в Java 8

- **Принцип работы:**
    
    - Ключ → `hashCode()` → индекс массива (bucket).
        
    - Коллизии: цепочки (LinkedList).
        
    - Вставка/поиск: O(1) в среднем.
        
- **Изменения в Java 8:**
    
    - При большом числе коллизий (более 8 элементов в бакете) список превращается в **красно-чёрное дерево**.
        
    - Это снижает время поиска с O(n) до O(log n).
        

## 94. Отличие `ArrayList` от `Vector`

|Характеристика|ArrayList|Vector|
|---|---|---|
|Потокобезопасность|Нет|Да (synchronized)|
|Производительность|Быстрее|Медленнее|
|Использование|Современный стандарт|Устарел, заменён на `Collections.synchronizedList` или `CopyOnWriteArrayList`|

## 95. Особенности интерфейса `Set`

- Хранит **уникальные элементы**.
    
- Не допускает дубликатов.
    
- Основан на `equals()` и `hashCode()`.
    
- Реализации:
    
    - `HashSet` — быстрая, без порядка.
        
    - `LinkedHashSet` — сохраняет порядок вставки.
        
    - `TreeSet` — сортированная.
        
- Используется, когда важна **уникальность** и иногда порядок.
    

## 📊 Итоговая шпаргалка

| Вопрос                  | Краткий ответ                                                           |
| ----------------------- | ----------------------------------------------------------------------- |
| Иерархия                | Iterable → Collection (List, Set, Queue), Map отдельно                  |
| Map                     | Пары ключ-значение, не наследует Collection                             |
| ArrayList vs LinkedList | ArrayList — быстрый доступ, LinkedList — быстрые вставки                |
| HashMap vs Hashtable    | HashMap быстрее, допускает null; Hashtable устарел                      |
| HashSet vs TreeSet      | HashSet — уникальность без порядка; TreeSet — уникальность + сортировка |
| HashMap Java 8          | Коллизии → дерево вместо списка                                         |
| ArrayList vs Vector     | ArrayList быстрее, Vector устарел                                       |
| Set                     | Уникальные элементы, разные реализации для порядка/сортировки           |
## 96. Как добавляются объекты в `HashSet`

- `HashSet` **основан на** `HashMap`:
    
    - Каждый элемент `HashSet` хранится как ключ в `HashMap`, а значение — константа `PRESENT`.
        
    - При добавлении объекта:
        
        1. Вызывается `hashCode()` у объекта.
            
        2. По хэшу вычисляется индекс бакета.
            
        3. Если в бакете нет такого объекта (по `equals()`), он добавляется.
            
        4. Если объект уже есть (равен по `equals()`), то новый не добавляется (уникальность).
            

👉 Таким образом, уникальность элементов в `HashSet` обеспечивается комбинацией `hashCode()` и `equals()`.

## 97. Способы сортировки коллекции (3 способа)

### 1. **Использовать** `Collections.sort()`

- Работает с `List`.
    
- Сортирует по естественному порядку (`Comparable`) или по заданному `Comparator`.
    

java

```
List<String> list = Arrays.asList("C", "A", "B");
Collections.sort(list); // [A, B, C]
Collections.sort(list, Comparator.reverseOrder()); // [C, B, A]
```

### 2. **Использовать метод** `List.sort()` **(Java 8+)**

- Более современный способ, встроенный в `List`.
    

java

```
list.sort(Comparator.naturalOrder());
list.sort((a, b) -> b.compareTo(a)); // обратный порядок
```

### 3. **Использовать Stream API**

- Позволяет гибко сортировать и собирать результат.
    

java

```
List<String> sorted = list.stream()
                          .sorted()
                          .toList(); // [A, B, C]

List<String> sortedDesc = list.stream()
                              .sorted(Comparator.reverseOrder())
                              .toList(); // [C, B, A]
```

## 📊 Итоговая шпаргалка

|Вопрос|Краткий ответ|
|---|---|
|Добавление в HashSet|Через `HashMap`: ключ = объект, значение = константа; уникальность по `hashCode` + `equals`|
|Сортировка коллекции|1) `Collections.sort()` 2) `List.sort()` 3) `Stream.sorted()`|
## 98. Коллекции из пакета `java.util.concurrent`. Их особенности

- **ConcurrentHashMap** — потокобезопасный аналог `HashMap`. Использует сегментированную блокировку (в новых версиях — CAS и узко‑гранулярные блокировки), что позволяет многим потокам одновременно читать и частично писать без полной блокировки.
    
- **CopyOnWriteArrayList / CopyOnWriteArraySet** — при модификации создают копию массива. Отлично подходят для сценариев с частыми чтениями и редкими изменениями.
    
- **BlockingQueue (ArrayBlockingQueue, LinkedBlockingQueue, PriorityBlockingQueue)** — поддерживают операции `put()` и `take()` с блокировкой, удобны для паттерна Producer–Consumer.
    
- **ConcurrentLinkedQueue / ConcurrentLinkedDeque** — неблокирующие очереди, используют lock‑free алгоритмы.
    
- **ConcurrentSkipListMap / ConcurrentSkipListSet** — потокобезопасные аналоги `TreeMap` и `TreeSet`, поддерживают сортировку и навигацию.
    

**Особенности:**

- Потокобезопасность без необходимости вручную синхронизировать.
    
- Более высокая производительность по сравнению с классическими синхронизированными коллекциями (`Vector`, `Hashtable`).
    
- Итераторы _weakly consistent_ — не кидают `ConcurrentModificationException`, но могут не видеть все изменени


## 99. Что происходит при добавлении в `ArrayList` нового элемента

- `ArrayList` хранит элементы во внутреннем массиве (`Object[] elementData`).
    
- При вызове `add(E e)`:
    
    1. Проверяется, хватает ли места в массиве.
        
    2. Если места нет, вызывается `grow()`, который увеличивает размер массива (обычно в 1.5 раза).
        
    3. Новый элемент вставляется в конец массива.
        
- **Амортизированная сложность**: `O(1)` для добавления в конец, но при расширении массива — `O(n)` из-за копирования элементов.
    
- При добавлении по индексу (`add(int index, E e)`) происходит сдвиг элементов вправо, что имеет сложность `O(n)`.
## 100. Thread-safe и non-thread-safe коллекции

- **Thread-safe (потокобезопасные):**
    
    - `Vector`, `Hashtable`, `ConcurrentHashMap`, `CopyOnWriteArrayList`, `BlockingQueue`, `Collections.synchronizedList()` и др.
        
    - Все операции синхронизированы или используют специальные механизмы (CAS, сегментные блокировки).
        
    - Минус: накладные расходы на синхронизацию, иногда слабая масштабируемость.
        
- **Non-thread-safe (непотокобезопасные):**
    
    - `ArrayList`, `HashMap`, `HashSet`, `LinkedList`, `StringBuilder`.
        
    - Быстрее в однопоточной среде, но при многопоточном доступе возможны гонки данных, неконсистентность и `ConcurrentModificationException`.
        

**Сравнение:**

| Характеристика     | Thread-safe коллекции         | Non-thread-safe коллекции |
| ------------------ | ----------------------------- | ------------------------- |
| Синхронизация      | Есть (встроенная)             | Нет                       |
| Производительность | Ниже (из-за блокировок)       | Выше в однопоточной среде |
| Итераторы          | Weakly consistent / fail-safe | Fail-fast                 |
| Примеры            | Vector, ConcurrentHashMap     | ArrayList, HashMap        |
Вчера

можешь помочь подготовить ответы для собеседования для java разработчика 98. Коллекции из пакета concurrent. Их особенности. 99. Что происходит при добавлении в ArrayList нового элемента и как это реализовано. 100. Thread-safe and non-thread safe collections. 101. Метод для преобразования потоко-небезопасной коллекции в потоко-безопасную.

**Вот готовые и структурированные ответы на вопросы 98–101 для собеседования Java‑разработчика. Они помогут тебе уверенно объяснить ключевые моменты про коллекции, их потокобезопасность и внутренние реализации.**

## 98. Коллекции из пакета `java.util.concurrent`. Их особенности

- **ConcurrentHashMap** — потокобезопасный аналог `HashMap`. Использует сегментированную блокировку (в новых версиях — CAS и узко‑гранулярные блокировки), что позволяет многим потокам одновременно читать и частично писать без полной блокировки.
    
- **CopyOnWriteArrayList / CopyOnWriteArraySet** — при модификации создают копию массива. Отлично подходят для сценариев с частыми чтениями и редкими изменениями.
    
- **BlockingQueue (ArrayBlockingQueue, LinkedBlockingQueue, PriorityBlockingQueue)** — поддерживают операции `put()` и `take()` с блокировкой, удобны для паттерна Producer–Consumer.
    
- **ConcurrentLinkedQueue / ConcurrentLinkedDeque** — неблокирующие очереди, используют lock‑free алгоритмы.
    
- **ConcurrentSkipListMap / ConcurrentSkipListSet** — потокобезопасные аналоги `TreeMap` и `TreeSet`, поддерживают сортировку и навигацию.
    

**Особенности:**

- Потокобезопасность без необходимости вручную синхронизировать.
    
- Более высокая производительность по сравнению с классическими синхронизированными коллекциями (`Vector`, `Hashtable`).
    
- Итераторы _weakly consistent_ — не кидают `ConcurrentModificationException`, но могут не видеть все изменения.
    

## 99. Что происходит при добавлении в `ArrayList` нового элемента

- `ArrayList` хранит элементы во внутреннем массиве (`Object[] elementData`).
    
- При вызове `add(E e)`:
    
    1. Проверяется, хватает ли места в массиве.
        
    2. Если места нет, вызывается `grow()`, который увеличивает размер массива (обычно в 1.5 раза).
        
    3. Новый элемент вставляется в конец массива.
        
- **Амортизированная сложность**: `O(1)` для добавления в конец, но при расширении массива — `O(n)` из-за копирования элементов.
    
- При добавлении по индексу (`add(int index, E e)`) происходит сдвиг элементов вправо, что имеет сложность `O(n)`.
    

## 100. Thread-safe и non-thread-safe коллекции

- **Thread-safe (потокобезопасные):**
    
    - `Vector`, `Hashtable`, `ConcurrentHashMap`, `CopyOnWriteArrayList`, `BlockingQueue`, `Collections.synchronizedList()` и др.
        
    - Все операции синхронизированы или используют специальные механизмы (CAS, сегментные блокировки).
        
    - Минус: накладные расходы на синхронизацию, иногда слабая масштабируемость.
        
- **Non-thread-safe (непотокобезопасные):**
    
    - `ArrayList`, `HashMap`, `HashSet`, `LinkedList`, `StringBuilder`.
        
    - Быстрее в однопоточной среде, но при многопоточном доступе возможны гонки данных, неконсистентность и `ConcurrentModificationException`.
        

**Сравнение:**

|Характеристика|Thread-safe коллекции|Non-thread-safe коллекции|
|---|---|---|
|Синхронизация|Есть (встроенная)|Нет|
|Производительность|Ниже (из-за блокировок)|Выше в однопоточной среде|
|Итераторы|Weakly consistent / fail-safe|Fail-fast|
|Примеры|Vector, ConcurrentHashMap|ArrayList, HashMap|

## 101. Метод для преобразования непотокобезопасной коллекции в потокобезопасную

- Используются **обёртки из класса** `Collections`:
    
    - `Collections.synchronizedList(new ArrayList<>())`
        
    - `Collections.synchronizedSet(new HashSet<>())`
        
    - `Collections.synchronizedMap(new HashMap<>())`.
        
- Эти методы возвращают синхронизированное представление коллекции.
    
- Важно: при итерации нужно вручную синхронизировать блок, например:
    
    java
    
    ```
    List<String> list = Collections.synchronizedList(new ArrayList<>());
    synchronized (list) {
        for (String s : list) {
            System.out.println(s);
        }
    }
    ```
    
- Для высоконагруженных систем лучше использовать коллекции из `java.util.concurrent` (например, `ConcurrentHashMap` вместо `synchronizedMap`).

## 102.Частые добавления и удаления: какую коллекцию выбрать и как они устроены

### Однопоточная среда

- **ArrayList:**
    
    - **Плюсы:** быстрый случайный доступ по индексу O(1), добавление в конец амортизированно O(1).
        
    - **Минусы:** вставки/удаления из середины/начала требуют сдвигов O(n).
        
    - **Устройство:** динамический массив; при переполнении растёт примерно на 1.5x, копируя содержимое.
        
- **LinkedList:**
    
    - **Плюсы:** вставки/удаления на концах O(1), удаления по итератору без сдвигов.
        
    - **Минусы:** доступ по индексу O(n).
        
    - **Устройство:** двусвязный список; операции удаления меняют ссылки узлов, без перекладывания массива.
        
- **Практический выбор:** если часто добавляете/удаляете в середине или на концах и редко обращаетесь по индексу — берите `LinkedList`. Если доступ по индексу важен и большинство операций — добавление в конец — берите `ArrayList`.
    

### Многопоточная среда

- **CopyOnWriteArrayList/Set:**
    
    - **Плюсы:** чтение без блокировок, итераторы без CME.
        
    - **Минусы:** каждая модификация копирует массив — хорошо, когда модификации редки, а чтения часты.
        
- **ConcurrentLinkedQueue/Deque:**
    
    - **Плюсы:** lock‑free для большинства операций, хороши для частых добавлений/удалений из концов.
        
    - **Минусы:** нет случайного доступа; ориентированы на очереди/деки.
        
- **BlockingQueue (Array/Linked/PriorityBlockingQueue):**
    
    - **Плюсы:** блокирующие `put/take`, идеальны для producer–consumer.
        
    - **Минусы:** накладные расходы на блокировку; не для высокочастотных безблокирующих сценариев.
        
- **ConcurrentHashMap (+ наборы на базе ключей):**
    
    - Для частых добавлений/удалений по ключу с конкурентным доступом — лучший выбор; тонкая блокировка и CAS дают высокую пропускную способность.
        

> Вывод: при частых модификациях лучше `LinkedList` в однопоточном коде, `ConcurrentLinkedQueue/Deque` или `ConcurrentHashMap` в многопоточном. `CopyOnWrite*` — только если изменений мало, а чтений много.

## Удаление при итерации: как сделать правильно

- **Итератор и его remove():** Используйте итератор коллекции и вызывайте `iterator.remove()` сразу после `iterator.next()`. Это корректный способ удалить текущий элемент без `ConcurrentModificationException` и без манипуляций с индексами:
    
    java
    
    ```
    Iterator<Book> it = books.iterator();
    while (it.hasNext()) {
        if (shouldRemove(it.next())) {
            it.remove();
        }
    }
    ```
    
    Работает для всех `Collection`; для списков доступен `ListIterator`, который также поддерживает `add()` во время обхода.
    
- **removeIf (Java 8+):** Лаконичный способ удалить все элементы по предикату; в `ArrayList` реализован эффективно: сначала помечает элементы, потом одним проходом сдвигает/удаляет, снижая количество операций сдвига:
    
    java
    
    ```
    names.removeIf(e -> e.startsWith("A"));
    ```
    
    Поведение согласовано для `ArrayList` и `LinkedList` в JDK 8+.
    
- **Стримы и копирование:** Если нужна «чистая» фильтрация без мутаций исходной коллекции:
    
    java
    
    ```
    List<Book> filtered = books.stream()
        .filter(b -> !shouldRemove(b))
        .collect(Collectors.toList());
    ```
    
    Помните про доп. расход памяти и необходимость заменить исходную ссылку или выполнить `removeAll`.
    
- **Особые случаи (слайсы):** Для отсортированных последовательных диапазонов у списков: `list.subList(from, to).clear();` — удаление подпоследовательности эффективно, так как `subList` «привязан» к исходному списку.
    

## Удаление из ArrayList (и других коллекций) при поиске в цикле

- **Проблема «пропуска» элементов при for‑индексном обходе:** При `data.remove(i)` элементы сдвигаются влево, следующий индекс `i+1` перескакивает на элемент после сдвига и часть элементов остаётся непроверенной. Поэтому при удалении по индексу проходите список с конца:
    
    java
    
    ```
    for (int i = data.size() - 1; i >= 0; i--) {
        if (match(data.get(i))) {
            data.remove(i);
        }
    }
    ```
    
    Или уменьшайте `i` после удаления, что сложнее и менее читабельно.
    
- **Правильный способ с итератором:**
    
    java
    
    ```
    Iterator<String> it = list.iterator();
    while (it.hasNext()) {
        if (it.next().equals("Basketball")) {
            it.remove();
            break; // если удаляем только первое вхождение
        }
    }
    ```
    
    Это предотвращает `ConcurrentModificationException` и корректно работает с внутренними структурами `ArrayList` и `LinkedList`.
    
- **removeIf для множественных совпадений (Java 8+):**
    
    java
    
    ```
    list.removeIf(p -> p.contains("_Hardi"));
    ```
    
    Простой и безопасный способ массового удаления элементов по условию, без ручного управления итератором.
    
- **Специфика структур:**
    
    - **ArrayList:** удаление по индексу сдвигает «хвост» массива; множественные удаления лучше выполнять либо обратным проходом, либо через `removeIf`. В JDK 21 появился `removeLast()` для удаления последнего элемента без вычисления индекса.
        
    - **LinkedList:** `Iterator.remove()` просто перенастраивает ссылки узлов; массовые удаления часто дешевле, чем у `ArrayList`, так как нет сдвигов массива.
        

> Практическое правило: во время поиска используйте `Iterator.remove()` или `removeIf`. Если по индексам — идите с конца. Не удаляйте из `ArrayList` по индексу в прямом цикле — получите пропуски и/или ошибки.

