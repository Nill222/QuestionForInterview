## 1. JVM, JDK, JRE

- **JVM (Java Virtual Machine)** Виртуальная машина, которая исполняет байт-код (`.class` файлы). Она обеспечивает переносимость: один и тот же байт-код работает на разных ОС.
    
- **JRE (Java Runtime Environment)** Среда выполнения Java. Включает JVM + стандартные библиотеки (rt.jar и др.). Нужна для запуска программ.
    
- **JDK (Java Development Kit)** Набор инструментов для разработки. Включает JRE + компилятор `javac`, отладчики, документацию.
    

👉 Взаимодействие: `JDK` → содержит `JRE` → внутри `JRE` работает `JVM`.

## 2. Компиляция и запуск класса через консоль

bash

```
javac MyClass.java   # компиляция, создаётся MyClass.class
java MyClass         # запуск, JVM ищет метод main()
```

Если класс в пакете, запускать нужно с учётом структуры каталогов:

bash

```
javac com/example/MyClass.java
java com.example.MyClass
```

## 3. JIT-компилятор

- **Just-In-Time Compiler** — часть JVM.
    
- Переводит часто используемый байт-код в машинный код во время выполнения.
    
- Ускоряет работу программы (горячие методы компилируются в нативный код).
    
- В отличие от `javac`, который компилирует в байт-код, JIT работает во время исполнения.

## 4. Classpath

- **Classpath** — путь, где JVM ищет классы и библиотеки.
    
- Если указаны две версии одной библиотеки: JVM загрузит **первую найденную** в classpath.
    
- Итог: порядок в classpath критичен.

## 5. Области памяти JVM

- **Heap (куча)** — хранение объектов, пул строк.
    
- **Stack (стек)** — хранение примитивов и ссылок в рамках вызова метода.
    
- **Metaspace (раньше PermGen)** — хранение метаданных классов.
    
- **PC Register** — хранит адрес текущей инструкции для каждого потока.
    
- **Native Method Stack** — стек для вызовов нативного кода (C/C++).

## 6. Пакеты

- Используются для организации кода и предотвращения конфликтов имён.
    
- Принцип именования: обычно доменное имя компании в обратном порядке: `com.company.project.module`.

## 7. Модификаторы доступа

- `public` — доступ отовсюду.
    
- `protected` — доступ внутри пакета + наследники.
    
- `default` (package-private) — доступ только внутри пакета.
    
- `private` — доступ только внутри класса.

## 8. Почему `main()` — `public static void`

- `public` — JVM должна иметь доступ извне.
    
- `static` — метод вызывается без создания объекта.
    
- `void` — не возвращает значение.
    
- Сигнатура: `public static void main(String[] args)`.
    

## 9. Package level access

- Это **default** доступ (без модификатора).
    
- Пример:
    

java

```
class A {
    int value; // доступен только внутри пакета
}
```

## 10. Доступ к `private` переменной

- Через **геттеры/сеттеры**.
    
- Через **reflection API** (например, `Field.setAccessible(true)`).
    
- Внутри самого класса.
    

## 11. Классы-оболочки и Integer Cache

- Оболочки: `Integer`, `Double`, `Boolean` и др. — объекты для примитивов.
    
- **Integer Cache**: значения от -128 до 127 кешируются.
    

java

```
Integer a = 100;
Integer b = 100;
System.out.println(a == b); // true
```

## 12. Autoboxing и Unboxing

- **Autoboxing**: примитив → объект.
    

java

```
Integer x = 5; // компилятор превращает в Integer.valueOf(5)
```

- **Unboxing**: объект → примитив.
    

java

```
int y = x; // компилятор вызывает x.intValue()
```

## 13. Задача

java

```
int a = 1;
Integer b = 2;
int c = a + b; // результат: 3
```

Если `Integer b = null` → при `a + b` произойдёт **NullPointerException** (unboxing null).

## 14. `var`

- Локальная переменная с выводом типа (Java 10+).
    

java

```
var list = new ArrayList<String>(); // тип: ArrayList<String>
```

- **Плюсы**: меньше кода, читаемость.
    
- **Минусы**: иногда теряется явность типа.
    

## 15. LocalDateTime, ZonedDateTime, OffsetDateTime, Instant

- **LocalDateTime** — дата и время без информации о зоне.
    
- **ZonedDateTime** — дата и время + часовой пояс.
    
- **OffsetDateTime** — дата и время + смещение от UTC.
    
- **Instant** — точка времени в формате UTC (количество секунд/наносекунд с 1970-01-01).