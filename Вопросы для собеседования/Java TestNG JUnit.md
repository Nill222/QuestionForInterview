## 65. Механизм `assert`

### 🔑 Что это

- В Java есть оператор `assert`, который используется для **проверки инвариантов и предположений в коде**.
    
- Синтаксис:
    
    java
    
    ```
    assert condition;
    assert condition : "Сообщение об ошибке";
    ```
    

### ⚙️ Когда возникает `AssertionError`

- Если условие в `assert` возвращает `false`, JVM выбрасывает исключение `AssertionError`.
    
- Пример:
    
    java
    
    ```
    int x = -1;
    assert x >= 0 : "x должен быть неотрицательным"; 
    // AssertionError: x должен быть неотрицательным
    ```
    

### 📊 Смысл

- Используется для **отладки и тестирования**, чтобы убедиться, что программа работает по ожидаемым правилам.
    
- В продакшене обычно отключается (по умолчанию `assert` выключен, включается флагом `-ea` при запуске JVM).
    
- Не предназначен для обработки ошибок пользователя, а именно для **проверки корректности логики разработчика**.
    

## 66. JUnit и TestNG

### 🔑 Что это

- **JUnit** и **TestNG** — это **фреймворки для модульного тестирования** в Java.
    
- Позволяют писать автоматические тесты, проверять корректность работы кода, интегрировать тесты в CI/CD.
    

### ⚙️ Принципы написания тестов

- **Аннотации:**
    
    - JUnit:
        
        - `@Test` — метод-тест.
            
        - `@BeforeEach` / `@AfterEach` — выполняются перед/после каждого теста.
            
        - `@BeforeAll` / `@AfterAll` — выполняются один раз для всего класса.
            
    - TestNG:
        
        - `@Test` — метод-тест.
            
        - `@BeforeMethod` / `@AfterMethod` — аналог JUnit.
            
        - `@BeforeClass` / `@AfterClass`.
            
        - Дополнительно: `@DataProvider` для параметризации тестов.
            
- **Assertions (проверки):**
    
    - JUnit: `Assertions.assertEquals(expected, actual)`, `assertTrue(condition)`, `assertThrows(...)`.
        
    - TestNG: `Assert.assertEquals(...)`, `Assert.assertTrue(...)`.
        
- **Организация:**
    
    - Тесты должны быть **маленькими, независимыми и повторяемыми**.
        
    - Один тест проверяет одну конкретную функциональность.
        
    - Используются **фикстуры** (инициализация окружения).
        
    - Хорошая практика — **имя теста должно отражать проверяемое условие**.
        

### 📊 Отличия JUnit vs TestNG

|Характеристика|JUnit|TestNG|
|---|---|---|
|Простота|Минималистичный|Более гибкий|
|Параметризация|Ограниченная|`@DataProvider`|
|Группировка тестов|Ограниченная|Есть (`groups`)|
|Популярность|Очень высокая|Чуть ниже, но часто в enterprise|
|Интеграция|Maven/Gradle, CI/CD|Maven/Gradle, CI/CD|

## 🚀 Итоговая шпаргалка

| Вопрос         | Краткий ответ                                                      |
| -------------- | ------------------------------------------------------------------ |
| `assert`       | Проверка инвариантов, выбрасывает `AssertionError` при `false`     |
| AssertionError | Возникает при нарушении предположений разработчика                 |
| JUnit/TestNG   | Фреймворки для модульного тестирования                             |
| Принципы       | Аннотации, assertions, независимые тесты, фикстуры, параметризация |