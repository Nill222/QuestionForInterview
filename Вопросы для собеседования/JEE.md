## **1. Что входит в Java EE (Jakarta EE)?**

📌 **Java EE** (сейчас называется **Jakarta EE**) — это набор **спецификаций** для enterprise-приложений.

### **Основные компоненты**

#### 🔹 Web

- **Servlet API**
    
- **JSP**
    
- **JSF**
    
- **WebSocket**
    

#### 🔹 Business

- **EJB** (Stateless, Stateful)
    
- **CDI** (Dependency Injection)
    

#### 🔹 Persistence

- **JPA**
    
- **JTA** (транзакции)
    

#### 🔹 Integration

- **JMS**
    
- **JAX-RS** (REST)
    
- **JAX-WS** (SOAP)
    

#### 🔹 Security

- JAAS
    
- Declarative Security
    

---

### **Важно**

- Java EE **не реализация**, а спецификации
    
- Реализации: **WildFly, GlassFish, WebLogic**
    

---

### **Интервью-формулировка**

> Java EE — это набор спецификаций для построения корпоративных приложений, включающий сервлеты, JPA, JAX-RS, JMS и другие технологии.

---

## **2. Какие бывают WebService?**

### **Основные типы**

#### 🔹 **SOAP**

- XML
    
- WSDL
    
- Строгий контракт
    
- Использует HTTP, SMTP и др.
    

#### 🔹 **REST**

- HTTP
    
- JSON / XML
    
- Stateless
    
- Легковесный
    

---

### **Сравнение**

|SOAP|REST|
|---|---|
|XML|JSON|
|Строгий|Гибкий|
|WSDL|OpenAPI|
|Тяжёлый|Лёгкий|

---

### **Java API**

- SOAP → **JAX-WS**
    
- REST → **JAX-RS**
    

---

### **Интервью**

> Основные типы веб-сервисов — SOAP и REST. REST более лёгкий и широко используется.

---

## **3. RESTful сервис. Принципы работы**

### **Что такое REST**

📌 **REST** — архитектурный стиль взаимодействия по HTTP.

---

### **Принципы REST**

1️⃣ **Client-Server**  
2️⃣ **Stateless** — сервер не хранит состояние клиента  
3️⃣ **Cacheable**  
4️⃣ **Uniform Interface**  
5️⃣ **Layered System**  
6️⃣ **Code on Demand** (опционально)

---

### **REST + HTTP**

|HTTP метод|Операция|
|---|---|
|GET|Получить|
|POST|Создать|
|PUT|Обновить|
|DELETE|Удалить|

---

### **Пример**

`GET /users/10`

---

### **Интервью**

> RESTful сервисы основаны на HTTP, используют ресурсы, стандартные методы и не хранят состояние клиента.

---

## **4. Сервер приложений и веб-сервер. Отличия**

---

### **Веб-сервер**

📌 Обрабатывает HTTP-запросы и отдаёт контент.

🔹 **Примеры**

- Nginx
    
- Apache HTTP Server
    

---

### **Сервер приложений**

📌 Выполняет бизнес-логику и Java EE спецификации.

🔹 **Примеры**

- WildFly
    
- GlassFish
    
- WebLogic
    
- TomEE
    

---

### **Отличия**

|Веб-сервер|Сервер приложений|
|---|---|
|Статический контент|Бизнес-логика|
|HTTP|Java EE|
|Быстрый|Тяжёлый|

---

### **Важно**

- Tomcat = **контейнер сервлетов**, не полноценный Java EE сервер
    

---

### **Интервью**

> Веб-сервер обрабатывает HTTP, сервер приложений реализует Java EE спецификации и выполняет бизнес-логику.

---

## **5. Контейнер сервлетов и сервлет**

---

### **Что такое сервлет**

📌 Java-класс, обрабатывающий HTTP-запросы и ответы.

`public class MyServlet extends HttpServlet {}`

---

### **Контейнер сервлетов**

📌 Среда выполнения сервлетов.

🔹 Отвечает за:

- жизненный цикл
    
- многопоточность
    
- безопасность
    
- mapping URL
    

---

### **Примеры контейнеров**

- Tomcat
    
- Jetty
    
- Undertow
    

---

### **Интервью**

> Контейнер сервлетов управляет жизненным циклом сервлетов и обработкой запросов.

---

## **6. Методы и жизненный цикл сервлета**

---

### **Жизненный цикл**

1️⃣ **init()** — инициализация (один раз)  
2️⃣ **service()** — обработка запросов  
3️⃣ **destroy()** — уничтожение

---

### **HTTP методы**

`doGet() doPost() doPut() doDelete()`

---

### **Важно**

- Один экземпляр сервлета
    
- Многопоточная обработка
    
- Нужно следить за thread-safety
    

---

### **Интервью**

> Сервлет создаётся один раз, затем обрабатывает запросы в нескольких потоках, и в конце уничтожается контейнером.

---

## **Частый вопрос-ловушка**

❓ _Можно ли хранить состояние в сервлете?_  
❌ **Нельзя** (race condition)


---

## **7. Что такое JSP. Жизненный цикл JSP**

### **Что такое JSP**

📌 **JSP (JavaServer Pages)** — технология для создания **динамических HTML-страниц** на сервере с использованием Java.

По сути:

> JSP — это **сервлет**, написанный в HTML-стиле.

---

### **Как работает JSP**

1️⃣ JSP → **компилируется в сервлет**  
2️⃣ Сервлет → компилируется в `.class`  
3️⃣ Выполняется контейнером

---

### **Жизненный цикл JSP**

1️⃣ **Translation** — JSP → Servlet  
2️⃣ **Compilation** — `.java` → `.class`  
3️⃣ **Loading**  
4️⃣ **jspInit()**  
5️⃣ **_jspService()**  
6️⃣ **jspDestroy()**

---

### **Интервью**

> JSP компилируется в сервлет при первом обращении и дальше живёт как обычный сервлет.

---

## **8. Что такое сессия? Жизненный цикл**

### **Что такое HTTP Session**

📌 **Session** — механизм хранения **состояния пользователя** между HTTP-запросами.

---

### **Как создаётся**

`HttpSession session = request.getSession();`

---

### **Жизненный цикл**

1️⃣ Создание при первом обращении  
2️⃣ Использование между запросами  
3️⃣ Инвалидация:

- `session.invalidate()`
    
- timeout
    
- закрытие браузера (не всегда)
    

---

### **Где хранится**

- На сервере
    
- Клиент хранит только `JSESSIONID`
    

---

### **Интервью**

> Сессия хранит состояние пользователя на сервере и идентифицируется с помощью cookie JSESSIONID.

---

## **9. Что такое request? Из чего состоит? Жизненный цикл**

---

### **Что такое request**

📌 **HttpServletRequest** — объект, представляющий HTTP-запрос клиента.

---

### **Из чего состоит**

- URL
    
- HTTP метод
    
- Headers
    
- Parameters
    
- Cookies
    
- Body
    
- Attributes
    

---

### **Жизненный цикл**

1️⃣ Создаётся контейнером  
2️⃣ Передаётся в сервлет  
3️⃣ Уничтожается после ответа

---

### **Интервью**

> HttpServletRequest существует только в рамках одного HTTP-запроса.

---

## **10. Cookies. Как достать? Если Cookies удалить, можно ли достать сессию?**

---

### **Что такое Cookie**

📌 Небольшие данные, хранящиеся на клиенте.

---

### **Как достать**

`Cookie[] cookies = request.getCookies();`

---

### **Связь сессии и cookies**

- Сессия идентифицируется через `JSESSIONID`
    
- Он хранится в cookie
    

---

### **Если cookies отключены**

✅ Можно — через **URL rewriting**

`;jsessionid=...`

---

### **Интервью**

> Если cookies отключены, контейнер может использовать URL rewriting для поддержки сессии.

---

## **11. Что писать в браузере, чтобы обратиться к сервлету? Можно ли вызвать метод сервлета напрямую?**

---

### **URL к сервлету**

`http://localhost:8080/app/myServlet`

(по mapping в web.xml или @WebServlet)

---

### **Можно ли вызвать метод напрямую?**

❌ **Нет**

- Браузер вызывает URL
    
- Контейнер вызывает `doGet()` / `doPost()`
    

---

### **Интервью**

> Браузер обращается к URL, а контейнер сам вызывает нужный метод сервлета.

---

## **12. Отличие POST и GET. Метод по умолчанию**

---

### **GET**

- Данные в URL
    
- Можно кэшировать
    
- Ограничение по длине
    

---

### **POST**

- Данные в body
    
- Не кэшируется
    
- Используется для изменения состояния
    

---

### **По умолчанию**

📌 **GET**

---

### **Интервью-таблица**

|GET|POST|
|---|---|
|URL|Body|
|Безопаснее? ❌|✅|
|Кэш|Да|

---

### **Фраза для интервью**

> Если метод не указан явно, браузер использует GET.

---

## **Частые ловушки**

❓ _Закрытие браузера = конец сессии?_  
👉 Не всегда (зависит от cookie)

❓ _POST безопаснее?_  
👉 Нет, просто данные не в URL

---


## **13. Отличие doPost() и doPut(). Идемпотентность**

### **doPost()**

📌 Используется для **создания ресурса**  
📌 Может менять состояние сервера  
📌 **НЕ идемпотентен**

`POST /users`

---

### **doPut()**

📌 Используется для **обновления / замены ресурса**  
📌 Идемпотентен

`PUT /users/10`

---

### **Идемпотентность**

📌 Повторный вызов даёт тот же результат.

---

### **Идемпотентные методы**

- GET
    
- PUT
    
- DELETE
    
- HEAD
    
- OPTIONS
    

❌ POST — не идемпотентный

---

### **Интервью**

> doPut идемпотентен, а doPost — нет, потому что повторный POST может создать несколько ресурсов.

---

## **14. Как сделать redirect незаметно для пользователя?**

❗ **Нельзя**

---

### **Почему**

- Redirect → HTTP 3xx
    
- Браузер **меняет URL**
    
- Пользователь это видит
    

---

### **Незаметная альтернатива**

📌 **forward**

`request.getRequestDispatcher("/page.jsp")        .forward(request, response);`

---

### **Интервью**

> Redirect всегда заметен, для незаметного перехода используется forward.

---

## **15. Scopes в JSP**

### **Области видимости**

|Scope|Где живёт|
|---|---|
|page|В пределах JSP|
|request|Один запрос|
|session|Сессия|
|application|Всё приложение|

---

### **Пример**

`<%     request.setAttribute("x", 10); %>`

---

### **Интервью**

> В JSP есть четыре области видимости: page, request, session и application.

---

## **16. Forward vs Redirect**

|Forward|Redirect|
|---|---|
|Сервер|Клиент|
|URL не меняется|URL меняется|
|Один запрос|Два запроса|
|Быстрее|Медленнее|

---

### **HTTP статус**

- Forward — 200
    
- Redirect — 302 / 301
    

---

### **Интервью**

> Forward выполняется на сервере, redirect — через браузер.

---

## **17. getAttribute() vs getParameter()**

---

### **getParameter()**

📌 Данные из запроса клиента

`request.getParameter("name");`

- String
    
- GET / POST
    

---

### **getAttribute()**

📌 Данные на сервере

`request.setAttribute("user", user);`

- Object
    
- используется между сервлетами / JSP
    

---

### **Интервью**

> getParameter читает данные запроса, getAttribute — серверные атрибуты.

---

## **18. Из чего состоит URL**

### **Структура**

`scheme://host:port/path?query#fragment`

---

### **Пример**

`https://example.com:8080/app/users?id=10#top`

---

### **Разбор**

- scheme — protocol
    
- host — домен
    
- port — порт
    
- path — путь
    
- query — параметры
    
- fragment — якорь
    

---

### **Интервью**

> URL состоит из протокола, хоста, порта, пути, query-параметров и фрагмента.

---

## **Частые ловушки**

❓ _PUT безопаснее POST?_  
👉 Нет, просто идемпотентен

❓ _Можно ли передать объект через redirect?_  
👉 Нет, только через URL


## **19. Отличие `<jsp:include>` от директивы `<%@ include %>`**

### **`<%@ include %>` — статический include**

📌 Подставляется **на этапе компиляции JSP**

`<%@ include file="header.jsp" %>`

- Один сервлет
    
- Общий namespace
    
- Быстрее
    
- Нельзя динамически менять
    

---

### **`<jsp:include>` — динамический include**

📌 Выполняется **во время запроса**

`<jsp:include page="header.jsp" />`

- Отдельный сервлет
    
- Свой request/response
    
- Можно передавать параметры
    

---

### **Таблица для интервью**

||`<%@ include %>`|`<jsp:include>`|
|---|---|---|
|Время|Compile time|Runtime|
|Сервлет|Один|Несколько|
|Динамика|❌|✅|

---

### **Интервью**

> Директива include работает на этапе компиляции, jsp:include — во время выполнения.

---

## **20. HttpServletRequestWrapper и HttpServletResponseWrapper**

### **Зачем нужны**

📌 Позволяют **оборачивать request/response** и изменять поведение **без изменения контейнера**.

---

### **Типичное применение**

- Фильтры
    
- Логирование
    
- Изменение параметров
    
- Обёртка body
    

---

### **Пример**

`class MyRequestWrapper extends HttpServletRequestWrapper {     public MyRequestWrapper(HttpServletRequest request) {         super(request);     } }`

---

### **Интервью**

> Wrapper’ы используются для перехвата и модификации request/response, чаще всего в фильтрах.

---

## **21. Что делает RequestDispatcher.include()**

📌 **Вставляет результат другого ресурса в текущий ответ**

---

### **Пример**

`request.getRequestDispatcher("/header.jsp")        .include(request, response);`

---

### **Отличие от forward**

- include → продолжает выполнение
    
- forward → прекращает выполнение
    

---

### **Интервью**

> include добавляет вывод другого ресурса в текущий response, не прерывая выполнение.

---

## **22. Последовательность выполнения servlet-фильтров**

### **Порядок**

📌 **В порядке объявления** (web.xml или @Order)

---

### **Цепочка**

`filter1 → filter2 → servlet ← filter2 ← filter1`

---

### **Пример**

`chain.doFilter(request, response);`

---

### **Интервью**

> Фильтры выполняются по цепочке, сначала до сервлета, потом обратно после него.

---

## **23. Как обрабатывается тег с телом? (JSP Custom Tag)**

📌 Вопрос про **JSP Custom Tags**

---

### **Жизненный цикл**

1️⃣ `doStartTag()`  
2️⃣ обработка тела  
3️⃣ `doEndTag()`

---

### **Опции**

- SKIP_BODY
    
- EVAL_BODY_INCLUDE
    

---

### **Интервью**

> Тег с телом сначала обрабатывает начало, затем тело и завершение через методы жизненного цикла.

---

## **24. JSTL**

### **Что такое JSTL**

📌 **JSP Standard Tag Library** — стандартная библиотека тегов.

---

### **Основные группы**

- core (`c:if`, `c:forEach`)
    
- fmt
    
- sql (не рекомендуется)
    
- xml
    
- functions
    

---

### **Пример**

`<c:forEach var="u" items="${users}">     ${u.name} </c:forEach>`

---

### **Зачем JSTL**

- Убирает Java-код из JSP
    
- Повышает читаемость
    
- MVC-friendly
    

---

### **Интервью**

> JSTL позволяет писать логику представления без Java-кода в JSP.

---

## **Частые ловушки**

❓ _include = forward?_  
👉 Нет

❓ _Можно ли менять request в фильтре?_  
👉 Да, через Wrapper

---

## **Итог**

Ты сейчас закрываешь **весь классический Java Web-блок**, который спрашивают на:

- Java Developer
    
- Backend Developer
    
- Spring Developer (как база)
    


## **25. Как обратиться к хосту с Tomcat и конкретному сервлету**

### **Базовая структура URL**

`http://<host>:<port>/<context-path>/<servlet-mapping>`

- **host** — домен или IP (например `localhost` или `192.168.1.10`)
    
- **port** — порт Tomcat (по умолчанию `8080`)
    
- **context-path** — название веб-приложения (обычно имя war файла без расширения)
    
- **servlet-mapping** — URL паттерн сервлета (`/myServlet`)
    

---

### **Пример**

Tomcat запущен на localhost:8080, приложение `myapp.war`, сервлет с mapping `/login`:

`http://localhost:8080/myapp/login`

---

### **Что такое www**

- **www** — часть домена (host), стандартный субдомен для веб-сайтов.
    
- Не влияет на порт или путь сервлета.
    

---

### **Где указывать порт**

- В URL после host через `:8080`
    
- Если порт по умолчанию 80 (HTTP), писать не обязательно
    

---

### **Интервью-формулировка**

> Чтобы обратиться к конкретному сервлету, пишем `http://host:port/context/servlet-mapping`. Порт указываем после host через двоеточие, www — это просто субдомен.

---

## **26. Можно ли определить сервлет без url-паттерна?**

- Да, можно **в web.xml**:
    

`<servlet>     <servlet-name>MyServlet</servlet-name>     <servlet-class>com.example.MyServlet</servlet-class> </servlet>`

- **Но:** без `<servlet-mapping>` к нему **не будет прямого URL**, т.е. нельзя вызвать напрямую через браузер.
    

✅ **Вывод:** сервлет без URL-паттерна создаётся контейнером, но не доступен извне. Используется для внутренних вызовов (`RequestDispatcher.forward/include`).

---

## **27. Что такое HTTP. Отличия HTTP 1.0 и HTTP 2**

### **Что такое HTTP**

📌 **HTTP** — протокол прикладного уровня для передачи ресурсов (HTML, JSON, картинки) между клиентом и сервером.

---

### **HTTP 1.0**

- Каждое соединение → один запрос
    
- Нет поддержки pipelining по умолчанию
    
- Только текстовый протокол
    

### **HTTP 2**

- Мультиплексирование: несколько запросов по одному TCP соединению
    
- Бинарный протокол
    
- Header compression (HPACK)
    
- Сервер может «пушить» ресурсы (server push)
    

---

### **Интервью**

> HTTP 2 быстрее, поддерживает несколько запросов за одно соединение, бинарный формат и сжатие заголовков, HTTP 1.0 — текстовый, одно соединение на запрос.

---

## **28. Сохраняет ли HTTP своё состояние?**

❌ **Нет**, HTTP — **stateless**

- Сервер не хранит информацию между запросами
    
- Состояние реализуется через:
    
    - Cookies
        
    - Session (JSESSIONID)
        
    - URL rewriting
        

---

### **Интервью**

> HTTP сам по себе не хранит состояние, состояние реализуется на уровне приложений.

---

## **29. Как сервер понимает, что сессия уже создана?**

- С помощью **JSESSIONID**
    
- Передаётся в **cookie** или через URL rewriting
    
- Когда приходит запрос с идентификатором:
    

`request.getSession(false)`

- Сервер ищет существующую сессию по id → если есть → не создаёт новую
    

---

### **Интервью**

> Контейнер использует cookie (JSESSIONID) или URL rewriting, чтобы определить существующую сессию пользователя.

---

## **30. Отличие аутентификации от авторизации**

||Аутентификация|Авторизация|
|---|---|---|
|Цель|Проверка, кто пользователь|Проверка, что разрешено делать|
|Пример|login/password|роль user/admin|
|Когда|Перед доступом|После аутентификации|
|Технологии|Basic Auth, OAuth|RBAC, ACL|

---

### **Интервью**

> Аутентификация — это проверка личности, авторизация — проверка прав.

---

## **31. Что происходит, когда пользователь нажимает кнопку**

### **Пошагово**

1️⃣ **Форма** заполняется в браузере  
2️⃣ Нажатие кнопки → **HTTP запрос** формируется (GET/POST)  
3️⃣ Браузер отправляет запрос на сервер (host:port/context/servlet-mapping)  
4️⃣ **Контейнер сервлетов** получает запрос → создаёт **HttpServletRequest и HttpServletResponse**  
5️⃣ Контейнер находит нужный сервлет по **URL mapping**  
6️⃣ Вызывает `doGet()` или `doPost()` сервлета  
7️⃣ Сервлет может использовать:

- request.getParameter() → данные формы
    
- session / cookies
    
- forward / redirect / JSP  
    8️⃣ Сервлет формирует ответ и отдаёт через **HttpServletResponse**  
    9️⃣ Браузер получает HTML/JSON/redirect → отображает пользователю
    

---

### **Интервью**

> Нажатие кнопки создаёт HTTP-запрос, контейнер оборачивает его в request/response, ищет сервлет по URL, вызывает метод doGet/doPost, сервлет формирует ответ и отправляет клиенту.