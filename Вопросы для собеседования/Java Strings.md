## 16. Разница между `new String()` и строковым литералом

- **Строковый литерал ("Hello")**
    
    - Сохраняется в **String Pool** (часть heap). permjem
        
    - Если строка уже есть в пуле, возвращается ссылка на существующий объект.
        
    - Экономия памяти и повышение производительности.
        
- `new String("Hello")`
    
    - Создаёт **новый объект** в куче, даже если такая строка уже есть в пуле.
        
    - Не использует кеширование.
        
    - Обычно не рекомендуется, кроме случаев, когда нужен гарантированно новый объект.
        

👉 Пример:

java

```
String s1 = "Hello";
String s2 = "Hello";
System.out.println(s1 == s2); // true (одна ссылка из пула)

String s3 = new String("Hello");
System.out.println(s1 == s3); // false (разные объекты)
```

## 17. Реализация класса `String`

- Класс `String` в Java:
    
    - **final** — нельзя наследовать.
        
    - Внутри хранит:
        
        - `private final char[] value;` (до Java 8)
            
        - С Java 9: `private final byte[] value;` + `coder` (для оптимизации хранения UTF-16/Latin-1).
            
    - Поле `hash` — кешированное значение хэш-кода.
        
    - Методы: `length()`, `charAt()`, `substring()`, `equals()`, `hashCode()`, `intern()` и др.
        

## 18. Как работает `substring()`

- Возвращает новую строку, представляющую часть исходной.
    
- До Java 7: `substring()` делал **share** массива символов (новая строка ссылалась на тот же массив `char[]` → риск утечки памяти).
    
- С Java 7u6: создаётся **новый массив символов**, копируется нужный диапазон.
    

java

```
String s = "HelloWorld";
String sub = s.substring(0, 5); // "Hello"
```

## 19. Unicode и UTF-8/UTF-16

- **Unicode** — стандарт кодирования символов (каждому символу присвоен уникальный код).
    
- **UTF-8**:
    
    - Переменная длина: 1–4 байта.
        
    - Совместим с ASCII (первые 128 символов = 1 байт).
        
    - Экономичен для текстов на английском.
        
- **UTF-16**:
    
    - Символы кодируются 2 или 4 байтами.
        
    - Более эффективен для языков с большим количеством символов (например, китайский).
        
- Отличие: UTF-8 компактнее для латиницы, UTF-16 — для иероглифов.
    

## 20. String, StringBuilder, StringBuffer

- **String**:
    
    - Иммутабельный (нельзя изменить содержимое).
        
    - Каждая операция конкатенации создаёт новый объект.
        
- **StringBuilder**:
    
    - Мутируемый (можно изменять содержимое).
        
    - Быстрее, чем `StringBuffer`, так как **не синхронизирован**.
        
    - Используется в однопоточных приложениях.
        
- **StringBuffer**:
    
    - Мутируемый.
        
    - **Синхронизирован** → безопасен для многопоточной среды.
        
    - Но медленнее, чем `StringBuilder`.
        

👉 Почему `final`?

- Чтобы гарантировать неизменность поведения (например, `String` всегда иммутабельный).
    
- Чтобы избежать наследования и изменения внутренней реализации, что могло бы нарушить безопасность и оптимизации (например, кеширование хэша).