## **19. Что такое нормализация?**

📌 **Нормализация** — процесс организации данных в базе так, чтобы:

1. Уменьшить **дублирование данных**
    
2. Избежать **аномалий вставки/обновления/удаления**
    
3. Упростить поддержку и интеграцию
    

---

### **Пример**

- Таблица `Orders` до нормализации:
    

|order_id|customer_name|product|price|
|---|---|---|---|
|1|Alice|Pen|10|
|2|Alice|Notebook|50|

- После нормализации: две таблицы `Customers` и `Orders`
    

|Customers|Orders|
|---|---|
|customer_id|name|
|1|Alice|
|2|Bob|

---

### **Интервью**

> Нормализация уменьшает дублирование данных и предотвращает аномалии.

---

## **20. Какие есть типы связей в базе данных? Пример**

### **Типы связей**

1️⃣ **One-to-One (1:1)**

- Каждая запись в таблице A соответствует одной записи в таблице B
    
- Пример: `User` ↔ `UserProfile`
    

2️⃣ **One-to-Many (1:N)**

- Одна запись в таблице A связана с множеством записей в таблице B
    
- Пример: `Author` → `Book`
    

3️⃣ **Many-to-Many (M:N)**

- Множество записей A ↔ множество записей B
    
- Через связующую таблицу
    
- Пример: `Student` ↔ `Course` через `Student_Course`
    

---

### **Интервью**

> Связи определяют, как таблицы связаны: 1:1, 1:N, M:N, часто реализуются через primary и foreign key.

---

## **21. Что такое Primary Key**

📌 **Primary Key (первичный ключ)** — уникальный идентификатор строки в таблице.

- Уникален
    
- Не может быть NULL
    
- Используется для ссылок других таблиц
    

`CREATE TABLE Users (     id INT PRIMARY KEY,     name VARCHAR(50) );`

---

### **Интервью**

> Primary key уникально идентифицирует запись и обеспечивает целостность таблицы.

---

## **22. Что такое Foreign Key**

📌 **Foreign Key (внешний ключ)** — ссылка на primary key другой таблицы.

`CREATE TABLE Orders (     order_id INT PRIMARY KEY,     user_id INT,     FOREIGN KEY (user_id) REFERENCES Users(id) );`

- Обеспечивает **ссылочную целостность**
    
- Позволяет связывать таблицы
    

---

### **Интервью**

> Foreign key — это ссылка на primary key другой таблицы, обеспечивает целостность данных.

---

## **23. Что такое индексы в базе данных**

### **Индекс** — структура данных для ускорения поиска в таблице

- Используется на колонках, по которым часто выполняются SELECT / JOIN / WHERE
    
- Типичные реализации: B-Tree, Hash
    

---

### **Плюсы**

- Быстрый поиск (O(log n) для B-Tree)
    
- Ускоряет сортировку и фильтрацию
    

### **Минусы**

- Замедляет INSERT/UPDATE/DELETE (надо обновлять индекс)
    
- Занимает дополнительное место
    

---

### **Интервью**

> Индексы ускоряют поиск и сортировку, но увеличивают расход памяти и замедляют модификацию данных.

---

## **24. Типы JOIN**

|JOIN|Описание|
|---|---|
|INNER JOIN|Берёт только совпадающие строки обеих таблиц|
|LEFT JOIN / LEFT OUTER JOIN|Все строки левой таблицы + совпадающие из правой (NULL если нет)|
|RIGHT JOIN / RIGHT OUTER JOIN|Все строки правой таблицы + совпадающие из левой|
|FULL OUTER JOIN|Все строки обеих таблиц, NULL где нет совпадений|
|CROSS JOIN|Декартово произведение (все комбинации)|

---

### **Пример**

`SELECT * FROM Users u LEFT JOIN Orders o ON u.id = o.user_id;`

- Возьмёт всех пользователей, даже без заказов
    

---

### **Интервью**

> JOIN используется для объединения таблиц: INNER — только совпадения, LEFT/RIGHT — все строки одной таблицы, FULL — все строки обеих таблиц.

---

## **25. Для чего используется HAVING. Отличие от WHERE**

- **WHERE** — фильтрует строки **до агрегации**
    
- **HAVING** — фильтрует **после группировки (GROUP BY)**
    

---

### **Пример**

`SELECT user_id, COUNT(*) as orders FROM Orders GROUP BY user_id HAVING COUNT(*) > 5;`

- WHERE нельзя использовать с агрегатными функциями
    
- HAVING — можно
    

---

### **Интервью**

> WHERE фильтрует исходные строки, HAVING — агрегированные группы после GROUP BY.

## **26. Зачем нужен VIEW и какие поля там будут**

### **Что такое VIEW**

📌 **VIEW** — виртуальная таблица, результат **SELECT-запроса**, который сохраняется как объект базы данных.

---

### **Зачем нужен**

- Упрощает сложные запросы
    
- Ограничивает доступ к данным (security)
    
- Сокращает дублирование кода
    
- Создаёт «абстракцию» над реальными таблицами
    

---

### **Какие поля будут**

- Любые, выбранные в SELECT
    
- Можно использовать `AS` для переименования
    

`CREATE VIEW HighSalary AS SELECT name, salary FROM Employee WHERE salary > 5000;`

- В `HighSalary` будут только поля `name` и `salary`
    

---

### **Интервью**

> VIEW — это виртуальная таблица для упрощения запросов и ограничения доступа к данным, содержит только выбранные поля SELECT.

---

## **27. Таблицы Customer и Order. Вывести всех customer, у которых суммарный заказ > 10000**

Предположим таблицы:

**Customer**: `customer_id, name`  
**Order**: `order_id, customer_id, amount`

---

### **SQL**

`SELECT c.customer_id, c.name, SUM(o.amount) as total FROM Customer c JOIN Order o ON c.customer_id = o.customer_id GROUP BY c.customer_id, c.name HAVING SUM(o.amount) > 10000;`

- `GROUP BY` — группировка по клиенту
    
- `SUM` — суммируем заказы
    
- `HAVING` — фильтр после агрегирования
    

---

### **Интервью**

> Используем JOIN + GROUP BY + HAVING для суммирования заказов и фильтрации клиентов с total > 10000.

---

## **28. Что такое агрегирующая функция, примеры**

📌 **Агрегатная функция** — функция, которая **сводит несколько строк в одно значение**.

### **Примеры**

- `SUM(column)` — сумма
    
- `COUNT(column)` — количество строк
    
- `AVG(column)` — среднее
    
- `MIN(column)` — минимум
    
- `MAX(column)` — максимум
    

`SELECT COUNT(*), AVG(salary) FROM Employee;`

- Работает вместе с `GROUP BY`
    

---

### **Интервью**

> Агрегирующие функции суммируют или подсчитывают значения по группе строк.

---

## **29. Примеры запросов с двумя таблицами**

### **Таблицы**

**Department**: `IDDepartment, Department`  
**Employer**: `IDEmployer, Salary, IDDepartment`

---

### **1. Выбрать Department, Salary, если сумма salary > 100, без JOIN**

`SELECT d.Department, e.Salary FROM Department d, Employer e WHERE d.IDDepartment = e.IDDepartment GROUP BY d.Department, e.Salary HAVING SUM(e.Salary) > 100;`

- Здесь используется **старый стиль JOIN через WHERE**
    
- GROUP BY обязательно для HAVING
    

---

### **2. Через JOIN**

`SELECT d.Department, e.Salary FROM Department d JOIN Employer e ON d.IDDepartment = e.IDDepartment GROUP BY d.Department, e.Salary HAVING SUM(e.Salary) > 100;`

- Новый стиль, явный JOIN
    
- Результат одинаковый, читается проще
    

---

### **Важно для интервью**

- JOIN = соединение таблиц
    
- GROUP BY + HAVING = фильтрация по агрегатам
    
- Старый стиль через WHERE тоже работает, но считается устаревшим