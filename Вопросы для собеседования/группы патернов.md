# 🔹 Порождающие паттерны (Creational)

## **Чем они занимаются**

👉 **Отвечают за создание объектов**  
👉 Скрывают логику создания  
👉 Уменьшают зависимость от `new`

Проще:

> _Как создать объект так, чтобы код был гибким и расширяемым._

---

### **Singleton**

📌 **Задача:** гарантировать **один экземпляр класса**

**Java SE:**

`Runtime runtime = Runtime.getRuntime();`

- JVM может иметь только один Runtime
    
- Управляет ресурсами JVM
    

🗣 **Как сказать на интервью**

> Singleton используется, когда нужен один глобальный объект, например Runtime в Java.

---

### **Factory Method**

📌 **Задача:** вынести создание объекта в отдельный метод

**Java SE:**

`Calendar calendar = Calendar.getInstance();`

- Клиент не знает, какой конкретно класс создаётся
    
- Выбор реализации происходит внутри
    

🗣 **Интервью**

> Factory Method инкапсулирует логику создания объекта и возвращает нужную реализацию.

---

### **Abstract Factory**

📌 **Задача:** создавать **семейства связанных объектов**

**Java SE:**

`DocumentBuilderFactory factory =         DocumentBuilderFactory.newInstance();`

- Можно заменить целую «фабрику» без изменения кода клиента
    

🗣 **Интервью**

> Abstract Factory используется для создания связанных объектов без привязки к конкретным классам.

---

### **Builder**

📌 **Задача:** создание **сложных объектов пошагово**

**Java SE:**

`StringBuilder sb = new StringBuilder()         .append("Hello")         .append(" World");`

- Убирает «телескопические конструкторы»
    
- Часто используется для immutable объектов
    

🗣 **Интервью**

> Builder упрощает создание сложных объектов с большим количеством параметров.

---

### **Prototype**

📌 **Задача:** создание объекта через **клонирование**

**Java SE:**

`Object.clone();`

- Быстрое копирование
    
- Используется, если `new` дорогой
    

🗣 **Интервью**

> Prototype позволяет создавать новые объекты путём клонирования существующих.

---

### 🔹 **Итог по порождающим**

> Порождающие паттерны отвечают за гибкое и безопасное создание объектов.

---

# 🔹 Структурные паттерны (Structural)

## **Чем они занимаются**

👉 **Как классы и объекты связываются между собой**  
👉 Как расширять функциональность **без изменения кода**

Проще:

> _Как красиво «склеить» объекты._

---

### **Adapter**

📌 **Задача:** привести один интерфейс к другому

**Java SE:**

`List<Integer> list = Arrays.asList(1, 2, 3);`

- Массив → List
    
- Интерфейсы несовместимы
    

🗣 **Интервью**

> Adapter позволяет использовать несовместимые интерфейсы.

---

### **Decorator (Wrapper)**

📌 **Задача:** динамически добавить поведение

**Java SE:**

`InputStream in =     new BufferedInputStream(         new FileInputStream("a.txt")     );`

- Каждый wrapper добавляет функциональность
    
- Все реализуют один интерфейс
    

🗣 **Интервью**

> Decorator позволяет расширять поведение объекта без изменения его класса.

---

### **Proxy**

📌 **Задача:** контролировать доступ к объекту

**Java SE / Spring:**

- `java.lang.reflect.Proxy`
    
- Hibernate Lazy Loading
    

🗣 **Интервью**

> Proxy выступает заместителем объекта и добавляет контроль доступа или дополнительную логику.

---

### **Composite**

📌 **Задача:** работать с объектами и группами одинаково

**Java SE:**

- Swing (`JComponent`)
    
- DOM
    

🗣 **Интервью**

> Composite позволяет работать с древовидными структурами единообразно.

---

### 🔹 **Итог по структурным**

> Структурные паттерны отвечают за композицию и расширение объектов.

---

# 🔹 Поведенческие паттерны (Behavioral)

## **Чем они занимаются**

👉 **Как объекты взаимодействуют между собой**  
👉 Как распределяется ответственность и логика

Проще:

> _Кто с кем и как общается._

---

### **Observer**

📌 **Задача:** уведомление подписчиков о событиях

**Java SE:**

- `PropertyChangeListener`
    
- `EventListener`
    

🗣 **Интервью**

> Observer реализует механизм подписки и уведомления.

---

### **Strategy**

📌 **Задача:** вынести алгоритмы в отдельные классы

**Java SE:**

`Collections.sort(list, comparator);`

- Comparator — стратегия сортировки
    

🗣 **Интервью**

> Strategy позволяет выбирать алгоритм во время выполнения.

---

### **Command**

📌 **Задача:** инкапсулировать действие как объект

**Java SE:**

- `Runnable`
    
- `Callable`
    

🗣 **Интервью**

> Command превращает действие в объект, что упрощает очереди и undo/redo.

---

### **Template Method**

📌 **Задача:** определить скелет алгоритма

**Java SE:**

`HttpServlet.doGet()`

- Подклассы реализуют части алгоритма
    

🗣 **Интервью**

> Template Method задаёт общий алгоритм, позволяя подклассам переопределять шаги.

---

### **Iterator**

📌 **Задача:** единый способ обхода коллекций

**Java SE:**

- `Iterator`
    
- `ListIterator`
    

🗣 **Интервью**

> Iterator скрывает внутреннюю структуру коллекции.

---

### 🔹 **Итог по поведенческим**

> Поведенческие паттерны управляют взаимодействием объектов и распределением ответственности.

---

# 🎯 Как ЭТО ИДЕАЛЬНО сказать на собеседовании (шаблон)

> Порождающие паттерны отвечают за создание объектов, структурные — за композицию и расширение, поведенческие — за взаимодействие и распределение логики. Java SE активно использует все эти паттерны, например Singleton в Runtime, Decorator в InputStream и Strategy в Comparator.