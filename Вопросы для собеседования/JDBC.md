119 как создать conection
## 1. Загрузка драйвера

- В современных версиях Java (JDBC 4.0+) драйвер загружается автоматически при наличии в classpath.
    
- Раньше требовалось явно вызвать:
    
    java
    
    ```
    Class.forName("com.mysql.cj.jdbc.Driver");
    ```
    
- **Важно:** драйвер должен быть подключён как зависимость (например, через Maven).
    

## 2. Создание `Connection`

- Используется класс `DriverManager`:
    
    java
    
    ```
    String url = "jdbc:mysql://localhost:3306/mydb";
    String user = "root";
    String password = "secret";
    
    Connection conn = DriverManager.getConnection(url, user, password);
    ```
    
- **URL формируется так:** `jdbc:<vendor>://<host>:<port>/<database>?params`
    

## 3. Создание `Statement` или `PreparedStatement`

- **Statement:** для простых запросов без параметров.
    
    java
    
    ```
    Statement stmt = conn.createStatement();
    ```
    
- **PreparedStatement:** для параметризованных запросов (безопаснее, защищает от SQL‑инъекций).
    
    java
    
    ```
    PreparedStatement ps = conn.prepareStatement(
        "SELECT * FROM users WHERE id = ?");
    ps.setInt(1, 10);
    ```
    

## 4. Выполнение запроса

- Для **SELECT**:
    
    java
    
    ```
    ResultSet rs = ps.executeQuery();
    while (rs.next()) {
        String name = rs.getString("name");
        int age = rs.getInt("age");
        System.out.println(name + " - " + age);
    }
    ```
    
- Для **INSERT/UPDATE/DELETE**:
    
    java
    
    ```
    int rows = ps.executeUpdate();
    System.out.println("Rows affected: " + rows);
    ```
    

## 5. Закрытие ресурсов

- Всегда закрываем `ResultSet`, `Statement`, `Connection` (или используем try‑with‑resources).
    
    java
    
    ```
    try (Connection conn = DriverManager.getConnection(url, user, password);
         PreparedStatement ps = conn.prepareStatement("...")) {
        // работа с БД
    } catch (SQLException e) {
        e.printStackTrace();
    }
    ```
    
- Это гарантирует освобождение ресурсов даже при исключениях.
    

## Итоговая последовательность

1. Подключить драйвер (автоматически или через `Class.forName`).
    
2. Получить `Connection` через `DriverManager.getConnection`.
    
3. Создать `Statement` или `PreparedStatement`.
    
4. Выполнить запрос (`executeQuery` или `executeUpdate`).
    
5. Обработать результат (`ResultSet`).
    
6. Закрыть ресурсы (желательно через try‑with‑resources).

120 закрытие 
Закрытие `Connection` — это критически важный шаг при работе с JDBC, потому что соединение с базой данных является дорогим ресурсом. Если его не закрыть, можно получить утечки соединений и исчерпание пула.

## Правильные способы закрытия `Connection`

### 1. Использовать **try-with-resources** (рекомендуемый способ)

- Начиная с Java 7, можно использовать конструкцию `try-with-resources`, которая автоматически закрывает все ресурсы, реализующие интерфейс `AutoCloseable` (а `Connection`, `Statement`, `ResultSet` его реализуют).
    
- Пример:
    
    java
    
    ```
    String url = "jdbc:mysql://localhost:3306/mydb";
    String user = "root";
    String password = "secret";
    
    try (Connection conn = DriverManager.getConnection(url, user, password);
         PreparedStatement ps = conn.prepareStatement("SELECT * FROM users");
         ResultSet rs = ps.executeQuery()) {
    
        while (rs.next()) {
            System.out.println(rs.getString("name"));
        }
    
    } catch (SQLException e) {
        e.printStackTrace();
    }
    ```
    
- Здесь `conn`, `ps`, и `rs` будут закрыты автоматически в обратном порядке выхода из блока.
    

### 2. Закрывать вручную в `finally`

- Если не используется `try-with-resources`, нужно закрывать соединение в блоке `finally`.
    
- Пример:
    
    java
    
    ```
    Connection conn = null;
    try {
        conn = DriverManager.getConnection(url, user, password);
        Statement stmt = conn.createStatement();
        ResultSet rs = stmt.executeQuery("SELECT * FROM users");
        // работа с результатами
    } catch (SQLException e) {
        e.printStackTrace();
    } finally {
        if (conn != null) {
            try {
                conn.close();
            } catch (SQLException e) {
                e.printStackTrace();
            }
        }
    }
    ```
    

### 3. Важно помнить

- **Закрывать нужно все ресурсы:** сначала `ResultSet`, потом `Statement`, и только потом `Connection`.
    
- **Не закрывать соединение слишком рано:** если используется пул соединений (например, HikariCP, C3P0), вызов `conn.close()` не уничтожает соединение, а возвращает его в пул.
    
- **Не хранить Connection как глобальную переменную:** соединение должно жить только в рамках операции/транзакции.
    

✅ На собеседовании можно ответить так: _"Правильный способ — использовать try-with-resources, чтобы_ `Connection`_,_ `Statement` _и_ `ResultSet` _закрывались автоматически. Если это невозможно, закрывать вручную в блоке finally, проверяя на null. В реальных проектах обычно применяется пул соединений, где_ `close()` _возвращает соединение в пул."_

## **122. Какие есть типы драйверов для соединения с СУБД?**

Речь идёт о **типах JDBC-драйверов**. Классическая классификация — **4 типа**.

### **Type 1 — JDBC-ODBC Bridge Driver**

📌 **Описание**

- JDBC → ODBC → СУБД
    
- Использует ODBC-драйвер, установленный в системе
    

📌 **Плюсы**

- Простота (раньше)
    

📌 **Минусы**

- Очень медленный
    
- Зависит от ОС
    
- Устарел
    

📌 **Статус**  
❌ **Удалён начиная с Java 8**

---

### **Type 2 — Native API Driver**

📌 **Описание**

- JDBC → Native API СУБД (через C/C++)
    
- Требует нативные библиотеки
    

📌 **Плюсы**

- Быстрее Type 1
    

📌 **Минусы**

- Платформозависим
    
- Сложен в установке
    

📌 **Используется**  
Редко, в старых корпоративных системах

---

### **Type 3 — Network Protocol Driver**

📌 **Описание**

- JDBC → Middleware (сервер) → СУБД
    
- Клиент не знает, с какой БД работает напрямую
    

📌 **Плюсы**

- Нет нативных библиотек
    
- Можно подключаться к разным БД
    

📌 **Минусы**

- Дополнительный сетевой слой
    
- Медленнее Type 4
    

📌 **Используется**  
Редко

---

### **Type 4 — Thin Driver (Pure Java)**

📌 **Описание**

- JDBC → протокол СУБД
    
- **Чистая Java**
    

📌 **Плюсы**  
✅ Самый быстрый  
✅ Платформонезависимый  
✅ Не требует нативных библиотек

📌 **Примеры**

- PostgreSQL
    
- MySQL
    
- Oracle Thin Driver
    

📌 **Используется**  
✅ **99% современных приложений**

---

### **Как правильно ответить на собеседовании**

> Существует 4 типа JDBC-драйверов:  
> Bridge (Type 1), Native (Type 2), Network (Type 3) и Thin (Type 4).  
> В современных приложениях используется Type 4, так как он написан на чистой Java, быстрый и не требует нативных библиотек.

---

## **123. Чем отличается Statement от PreparedStatement?**

### **Statement**

`Statement stmt = connection.createStatement(); ResultSet rs = stmt.executeQuery(     "SELECT * FROM users WHERE id = " + id );`

📌 **Особенности**

- SQL формируется **как строка**
    
- Каждый раз запрос **парсится и компилируется заново**
    
- Уязвим к **SQL Injection**
    

---

### **PreparedStatement**

`PreparedStatement ps = connection.prepareStatement(     "SELECT * FROM users WHERE id = ?" ); ps.setInt(1, id); ResultSet rs = ps.executeQuery();`

📌 **Особенности**

- SQL компилируется **один раз**
    
- Параметры передаются отдельно
    
- Защита от SQL Injection
    
- Лучшая производительность при повторных вызовах
    

---

### **Где сохраняется запрос после первого вызова PreparedStatement?**

❗ Очень важный момент для интервью:

- SQL **предкомпилируется и кэшируется на стороне СУБД**
    
- JDBC-драйвер может дополнительно кешировать план выполнения
    
- При повторном выполнении используется **готовый execution plan**
    

📌 **Итог**

> PreparedStatement хранит скомпилированный запрос в БД, а не в JVM.

---

### **Будет ли тот же эффект, если формировать строку и отправлять в Statement?**

❌ **Нет**

Почему:

- Даже если SQL выглядит одинаково, для БД это **новый запрос**
    
- План выполнения создаётся **каждый раз**
    
- Нет защиты от SQL Injection
    

📌 **Пример атаки**

`String id = "1 OR 1=1";`

---

### **Ключевая таблица отличий**

|Критерий|Statement|PreparedStatement|
|---|---|---|
|Компиляция|Каждый раз|Один раз|
|SQL Injection|❌|✅|
|Производительность|Ниже|Выше|
|Параметры|Строкой|Через setters|

---

### **Как хотят услышать на интервью**

> PreparedStatement безопаснее, быстрее при повторном использовании и защищает от SQL-инъекций, так как параметры передаются отдельно от SQL-запроса.

---

## **124. Зачем нужен CallableStatement?**

📌 **Назначение**  
Используется для **вызова хранимых процедур и функций**.

---

### **Пример**

`CallableStatement cs =     connection.prepareCall("{call get_user_name(?, ?)}");  cs.setInt(1, 10); cs.registerOutParameter(2, Types.VARCHAR);  cs.execute();  String name = cs.getString(2);`

---

### **Когда использовать**

- Сложная бизнес-логика в БД
    
- Массовые операции
    
- Нужно OUT-параметры
    

---

### **Отличие от PreparedStatement**

||PreparedStatement|CallableStatement|
|---|---|---|
|SQL|Запрос|Хранимая процедура|
|OUT параметры|❌|✅|
|Возвращаемые значения|ResultSet|ResultSet + OUT|

---

### **На интервью**

> CallableStatement нужен для вызова хранимых процедур и функций, включая работу с входными и выходными параметрами.

---

## **125. Отличие executeUpdate() от executeQuery()**

### **executeQuery()**

📌 Используется для:

`SELECT`

📌 Возвращает:

`ResultSet`

---

### **executeUpdate()**

📌 Используется для:

`INSERT, UPDATE, DELETE, DDL`

📌 Возвращает:

`int // количество затронутых строк`

---

### **Пример**

`int rows = stmt.executeUpdate(     "UPDATE users SET active = true" );`

---

### **execute() — бонус**

`boolean hasResultSet = stmt.execute(sql);`

- true → есть ResultSet
    
- false → есть update count
    

---

### **Типичный вопрос с подвохом**

❓ _Можно ли executeUpdate() для SELECT?_  
❌ **Нет**, будет SQLException.

---

## **Краткий «идеальный» ответ для интервью**

> executeQuery используется для SELECT и возвращает ResultSet, executeUpdate — для DML и DDL запросов и возвращает количество изменённых строк.

## **126. Как в объекте ResultSet вернуться в предыдущую строку? Всегда ли можно вернуться?**

### **Короткий ответ**

➡️ Можно вызвать `previous()`, **НО не всегда** — зависит от типа `ResultSet`.

---

### **Типы ResultSet**

|Тип|Можно двигаться назад?|
|---|---|
|TYPE_FORWARD_ONLY|❌ Нет|
|TYPE_SCROLL_INSENSITIVE|✅ Да|
|TYPE_SCROLL_SENSITIVE|✅ Да|

---

### **Пример**

`Statement stmt = connection.createStatement(     ResultSet.TYPE_SCROLL_INSENSITIVE,     ResultSet.CONCUR_READ_ONLY );  ResultSet rs = stmt.executeQuery("SELECT * FROM users");  rs.next(); rs.next(); rs.previous(); // ✅ вернёмся назад`

---

### **Почему нельзя всегда?**

- По умолчанию создаётся `TYPE_FORWARD_ONLY`
    
- Такой ResultSet оптимизирован по памяти и скорости
    
- Драйвер может стримить данные
    

---

### **Вывод для интервью**

> Вернуться в предыдущую строку можно только в scrollable ResultSet, при типе TYPE_FORWARD_ONLY это невозможно.

---

## **127. Как получить сгенерированный СУБД первичный ключ без доп. запроса?**

📌 Используется `getGeneratedKeys()`

---

### **Пример**

`PreparedStatement ps = connection.prepareStatement(     "INSERT INTO users(name) VALUES (?)",     Statement.RETURN_GENERATED_KEYS );  ps.setString(1, "Alex"); ps.executeUpdate();  ResultSet keys = ps.getGeneratedKeys(); if (keys.next()) {     long id = keys.getLong(1); }`

---

### **Как это работает**

- СУБД генерирует ключ (AUTO_INCREMENT / SEQUENCE)
    
- JDBC-драйвер возвращает его в `ResultSet`
    

---

### **Важно**

- Работает не со всеми драйверами одинаково
    
- Для некоторых БД можно указать имя колонки
    

---

### **Как хотят услышать**

> После выполнения INSERT можно вызвать getGeneratedKeys(), который вернёт ResultSet со сгенерированными первичными ключами.

---

## **128. Транзакции. Принципы ACID**

### **Что такое транзакция**

📌 Логическая единица работы с БД, выполняемая **целиком или никак**.

---

### **ACID**

#### **A — Atomicity (Атомарность)**

- Либо всё выполняется
    
- Либо ничего не выполняется
    

`commit / rollback`

---

#### **C — Consistency (Согласованность)**

- БД всегда остаётся в валидном состоянии
    
- Ограничения (PK, FK, NOT NULL) не нарушаются
    

---

#### **I — Isolation (Изоляция)**

- Транзакции не мешают друг другу
    
- Есть разные уровни изоляции
    

---

#### **D — Durability (Надёжность)**

- После commit данные не потеряются
    
- Даже при сбое системы
    

---

### **Интервью-фраза**

> ACID — это набор свойств транзакций, гарантирующих атомарность, согласованность, изоляцию и надёжность.

---

## **129. Уровни изоляции транзакций. Виды чтения. Точки сохранения**

### **Виды проблем чтения**

#### **Dirty Read**

- Чтение незакоммиченных данных
    

#### **Non-repeatable Read**

- Повторное чтение даёт другой результат
    

#### **Phantom Read**

- Появляются новые строки при повторном запросе
    

---

### **Уровни изоляции**

|Уровень|Dirty|Non-repeatable|Phantom|
|---|---|---|---|
|READ_UNCOMMITTED|✅|✅|✅|
|READ_COMMITTED|❌|✅|✅|
|REPEATABLE_READ|❌|❌|✅|
|SERIALIZABLE|❌|❌|❌|

---

### **Как задать в JDBC**

`connection.setTransactionIsolation(     Connection.TRANSACTION_REPEATABLE_READ );`

---

### **Точки сохранения (Savepoint)**

📌 Позволяют откатиться **частично**

`Savepoint sp = connection.setSavepoint(); // ... connection.rollback(sp);`

---

### **Интервью**

> Savepoint позволяет откатить транзакцию до определённого момента без полного rollback.

---

## **130. SQL Injection. Как работает и как защищаться?**

### **Что это**

📌 Внедрение SQL-кода через пользовательский ввод.

---

### **Пример атаки**

`String sql = "SELECT * FROM users WHERE name = '" + input + "'";`

Ввод:

`' OR '1'='1`

Результат:

`SELECT * FROM users WHERE name = '' OR '1'='1'`

---

### **Как защищаться**

#### ✅ **PreparedStatement**

- Параметры не интерпретируются как SQL
    

`ps.setString(1, input);`

#### ✅ **Валидация входных данных**

- Белые списки
    
- Проверка формата
    

#### ❌ **НЕ помогает**

- Экранирование вручную
    
- String.replace()
    

---

### **Дополнительно**

- Ограничение прав БД пользователя
    
- ORM (Hibernate, JPA)
    

---

### **Идеальный ответ**

> SQL-инъекция — это атака, при которой злоумышленник внедряет SQL-код через ввод. Защита — использование PreparedStatement, валидация данных и минимальные права доступа.