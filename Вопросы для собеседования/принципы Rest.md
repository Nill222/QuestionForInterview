# Принципы REST (развёрнуто)

---

## **1️⃣ Client–Server**

### **Суть**

Клиент и сервер **жёстко разделены по ответственности**:

- **Клиент**
    
    - UI
        
    - UX
        
    - Отображение данных
        
- **Сервер**
    
    - Бизнес-логика
        
    - Работа с БД
        
    - Безопасность
        

---

### **Что это даёт**

✔ Независимое развитие клиента и сервера  
✔ Можно менять frontend, не трогая backend  
✔ Повышает масштабируемость

---

### **Пример**

- Backend: Spring Boot REST API
    
- Frontend: React / Angular / мобильное приложение
    

---

### **Как сказать на интервью**

> Client-Server разделяет ответственность между клиентом и сервером, что упрощает масштабирование и независимую разработку.

---

## **2️⃣ Stateless**

### **Суть**

📌 **Сервер НЕ хранит состояние клиента между запросами**

Каждый HTTP-запрос:

- самодостаточный
    
- содержит всю нужную информацию
    

---

### **Что хранится**

❌ НЕ хранится:

- session
    
- пользовательский state
    

✅ Передаётся:

- JWT / токен
    
- параметры запроса
    
- заголовки
    

---

### **Пример**

`GET /orders Authorization: Bearer eyJhbGciOi...`

---

### **Плюсы**

✔ Простое масштабирование  
✔ Можно легко балансировать нагрузку  
✔ Нет привязки клиента к конкретному серверу

---

### **Минусы**

❌ Каждый запрос тяжелее  
❌ Всё состояние нужно передавать каждый раз

---

### **Важно на интервью**

> Stateless — ключевое отличие REST от классических server-side приложений с HttpSession.

---

### **Интервью-фраза**

> Stateless означает, что сервер не хранит состояние клиента, и каждый запрос содержит всю необходимую информацию.

---

## **3️⃣ Cacheable**

### **Суть**

Ответ сервера **должен явно указывать**, можно ли его кешировать.

---

### **Как**

Через HTTP-заголовки:

`Cache-Control: max-age=3600 ETag: "abc123"`

---

### **Зачем**

✔ Снижение нагрузки на сервер  
✔ Быстрые ответы клиенту  
✔ Экономия трафика

---

### **Пример**

- GET `/products` — кешируется
    
- POST `/order` — не кешируется
    

---

### **Интервью**

> REST поддерживает кеширование ответов, что улучшает производительность и снижает нагрузку.

---

## **4️⃣ Uniform Interface** ⭐ (САМЫЙ ВАЖНЫЙ)

Это **сердце REST**. Часто спрашивают именно его.

---

### **Uniform Interface состоит из 4 частей**

---

### **4.1 Resource-based**

📌 Всё — **ресурсы**, а не действия

❌ Плохо:

`/getUser /createOrder`

✅ Хорошо:

`/users /orders`

---

### **4.2 Manipulation via representations**

📌 Работа через представления ресурса

- JSON / XML
    
- HTTP методы управляют состоянием
    

`POST /orders PUT /orders/10`

---

### **4.3 Self-descriptive messages**

📌 Сообщения самодокументируемые

- HTTP method
    
- HTTP status
    
- Headers
    
- Content-Type
    

`HTTP/1.1 201 Created Content-Type: application/json`

---

### **4.4 HATEOAS**

📌 **Hypermedia as the Engine of Application State**

Ответ содержит ссылки на возможные действия:

`{   "id": 10,   "status": "CREATED",   "links": [     { "rel": "cancel", "href": "/orders/10/cancel" }   ] }`

---

### **Интервью**

> Uniform Interface унифицирует взаимодействие с ресурсами через HTTP-методы, форматы данных и статусы.

---

## **5️⃣ Layered System**

### **Суть**

Архитектура может состоять из **слоёв**, но клиент об этом не знает.

---

### **Пример**

`Client → API Gateway → Auth → Service → DB`

---

### **Зачем**

✔ Безопасность  
✔ Масштабирование  
✔ Балансировка нагрузки

---

### **Интервью**

> Layered System позволяет добавлять промежуточные уровни, не изменяя клиента.

---

## **6️⃣ Code on Demand (опционально)**

### **Суть**

Сервер может **передавать исполняемый код клиенту**

---

### **Пример**

- JavaScript в браузере
    
- SPA-приложения
    

---

### **Важно**

- **Необязательный принцип**
    
- Часто не используется в REST API
    

---

### **Интервью**

> Code on Demand позволяет серверу расширять функциональность клиента, передавая исполняемый код.

---

# 🔹 Краткая таблица (шпаргалка)

|Принцип|Кратко|
|---|---|
|Client-Server|Разделение ответственности|
|Stateless|Сервер не хранит состояние|
|Cacheable|Ответы можно кешировать|
|Uniform Interface|Единый интерфейс|
|Layered System|Многоуровневая архитектура|
|Code on Demand|Исполняемый код клиенту|

---

# 🎯 Идеальный ответ на собеседовании (короткая версия)

> REST — это архитектурный стиль, основанный на принципах Client-Server, Stateless, Cacheable, Uniform Interface, Layered System и опционально Code on Demand. Ключевой принцип — Uniform Interface, который определяет работу с ресурсами через HTTP-методы, форматы и статусы.