`Thread` — это **объект потока**, который может выполнять задачу параллельно с другими потоками.  
Чтобы создать поток, можно либо:

- унаследоваться от `Thread`, переопределив `run()`
    
- передать объект `Runnable` в конструктор `Thread`.
    

class MyThread extends Thread {
    @Override
    public void run() {
        System.out.println("Работаю в отдельном потоке: " + Thread.currentThread().getName());
    }
}

public class Main {
    public static void main(String[] args) {
        Thread t = new MyThread();
        t.start(); // запускаем новый поток
    }
}


⚠️ Важно: вызов `t.run()` — **не запускает новый поток**, а выполняет код в текущем. Только `t.start()` создаёт новый поток.

---

### Runnable

`Runnable` — это **функциональный интерфейс** (один метод `run()`), который можно использовать с `Thread`.

public class Main {
    public static void main(String[] args) {
        Runnable task = () -> {
            System.out.println("Выполняется Runnable: " + Thread.currentThread().getName());
        };

        Thread t = new Thread(task);
        t.start();
    }
}


---

### ExecutorService

`ExecutorService` — это **пул потоков**.  
Он управляет количеством потоков, переиспользует их и позволяет планировать выполнение задач.

import java.util.concurrent.*;

public class Main {
    public static void main(String[] args) {
        ExecutorService executor = Executors.newFixedThreadPool(2);

        executor.submit(() -> {
            System.out.println("Задача 1 выполняется в " + Thread.currentThread().getName());
        });

        executor.submit(() -> {
            System.out.println("Задача 2 выполняется в " + Thread.currentThread().getName());
        });

        executor.shutdown(); // корректно завершаем работу
    }
}


---

# 🔹 2. `synchronized`, `volatile`, `wait/notify`

### synchronized

Гарантирует **взаимное исключение** (mutual exclusion) — только один поток может выполнять блок/метод в один момент времени. Также обеспечивает **видимость изменений** между потоками.

class Counter {
    private int count = 0;

    public synchronized void increment() {
        count++;
    }

    public synchronized int getCount() {
        return count;
    }
}


---

### volatile

Ключевое слово `volatile` гарантирует, что **значение переменной всегда читается из памяти**, а не из кеша процессора потока. Но **не даёт атомарности операций**.

class Example {
    private volatile boolean running = true;

    public void run() {
        while (running) {
            // бесконечный цикл
        }
    }

    public void stop() {
        running = false; // другой поток увидит изменение
    }
}


---

### wait/notify

Используются для **синхронизации потоков через монитор объекта**.  
`wait()` — поток засыпает, пока другой не вызовет `notify()` или `notifyAll()`.

class SharedResource {
    private boolean available = false;

    public synchronized void waitForResource() throws InterruptedException {
        while (!available) {
            wait(); // ждём
        }
        System.out.println("Ресурс получен: " + Thread.currentThread().getName());
    }

    public synchronized void releaseResource() {
        available = true;
        notifyAll(); // разбудим всех
    }
}

public class Main {
    public static void main(String[] args) throws InterruptedException {
        SharedResource resource = new SharedResource();

        new Thread(() -> {
            try {
                resource.waitForResource();
            } catch (InterruptedException e) {
                e.printStackTrace();
            }
        }).start();

        Thread.sleep(1000);

        new Thread(resource::releaseResource).start();
    }
}


---

# 🔹 3. Deadlock и Race condition

### Deadlock (взаимная блокировка)

Возникает, когда потоки ждут друг друга бесконечно.

class A {}
class B {}

public class DeadlockDemo {
    public static void main(String[] args) {
        final A a = new A();
        final B b = new B();

        Thread t1 = new Thread(() -> {
            synchronized (a) {
                System.out.println("Поток 1 захватил A");
                synchronized (b) {
                    System.out.println("Поток 1 захватил B");
                }
            }
        });

        Thread t2 = new Thread(() -> {
            synchronized (b) {
                System.out.println("Поток 2 захватил B");
                synchronized (a) {
                    System.out.println("Поток 2 захватил A");
                }
            }
        });

        t1.start();
        t2.start();
    }
}
    } }`

💡 Избежать можно:

- захватывать блокировки в одном порядке
    
- использовать `tryLock()` из `ReentrantLock`
    
- избегать излишней синхронизации.
    

---

### Race condition (гонка данных)

Возникает, когда несколько потоков обращаются к одной переменной без синхронизации.

class Counter {
    private int count = 0;

    public void increment() {
        count++; // НЕ атомарно: (чтение + увеличение + запись)
    }

    public int getCount() {
        return count;
    }
}


💡 Решение:

- `synchronized`
    
- атомарные классы (`AtomicInteger`)
    
- `Lock`.
    

---

# 🔹 4. Future и Callable

### Callable

Похож на `Runnable`, но:

- возвращает результат
    
- может выбросить исключение
    

  import java.util.concurrent.*;

public class CallableExample {
    public static void main(String[] args) throws Exception {
        Callable<Integer> task = () -> {
            System.out.println("Выполняем задачу в " + Thread.currentThread().getName());
            Thread.sleep(1000);
            return 42; // результат
        };

        ExecutorService executor = Executors.newSingleThreadExecutor();
        Future<Integer> future = executor.submit(task);

        System.out.println("Ожидание результата...");
        Integer result = future.get(); // блокирует, пока результат не готов
        System.out.println("Результат: " + result);

        executor.shutdown();
    }
}


---

### Future

`Future` — это **обёртка для результата асинхронного вычисления**. Позволяет:

- получить результат `get()`
    
- проверить готовность `isDone()`
    
- отменить выполнение `cancel()`
    

Future<Integer> future = executor.submit(task);

if (!future.isDone()) {
    System.out.println("Задача ещё выполняется...");
}

Integer result = future.get(); // блокирующий вызов



## 1. **Синхронизация метода экземпляра**

class Counter {
    private int count = 0;

    public synchronized void increment() {
        count++;
    }
}


🔑 Что происходит:

- Ключевое слово `synchronized` на **методе экземпляра** привязывается к **монитору текущего объекта (`this`)**.
    
- Это значит, что только один поток в данный момент может выполнять **любой synchronized-метод этого объекта**.
    

---

## 2. **Синхронизация метода класса (static)**

class Counter {
    private static int count = 0;

    public static synchronized void increment() {
        count++;
    }
}


🔑 Что происходит:

- Если метод статический, то синхронизация идёт по **монитору объекта `Class`** (т.е. `Counter.class`).
    
- Таким образом, синхронизация работает **для всех экземпляров класса одновременно**.
    

---

## 3. **Синхронизированный блок внутри метода**

class Counter {
    private int count = 0;

    public void increment() {
        synchronized (this) { // монитор конкретного объекта
            count++;
        }
    }
}

🔑 Что происходит:

- Синхронизация действует **только внутри блока**.
    
- Можно явно указать объект-монитор, например:
    
    `synchronized (lockObject) {     // критическая секция }`
    
- Это гибче, чем синхронизировать весь метод, потому что блок может быть меньше, а значит — меньше шанс "тормозов".
    

---

## 4. **Синхронизация на уровне класса (блок с Class)**

class Counter {
    private static int count = 0;

    public void increment() {
        synchronized (Counter.class) { // монитор класса
            count++;
        }
    }
}


🔑 Что происходит:

- Это аналог `static synchronized`, но более гибкий.
    
- Можно внутри метода блокировать **весь класс** (даже если метод нестатический).
    

---

## 5. **Синхронизация конструктора**

⚠️ В Java **нельзя** сделать конструктор `synchronized` напрямую, потому что при создании объекта на него ещё никто не ссылается. Но можно синхронизировать блок внутри конструктора:

class Example {
    public Example() {
        synchronized (this) {
            // инициализация с защитой
        }
    }
}

---

## Итог: к чему можно привязать `synchronized`

- **Метод экземпляра** → монитор объекта (`this`)
    
- **Статический метод** → монитор класса (`ClassName.class`)
    
- **Блок кода** → любой объект-монитор (`this`, `lockObject`, `ClassName.class` и т.п.)
    
- **Конструктор** → только через `synchronized`-блок
    

---

👉 На собеседованиях часто спрашивают:

- _"В чём разница между `synchronized` у метода и у блока?"_
    
- Ответ: у метода монитор фиксирован (`this` или `ClassName.class`), у блока — можно выбрать.