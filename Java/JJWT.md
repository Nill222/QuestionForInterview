Эти импорты относятся к библиотеке **JJWT (Java JWT)** — это популярная Java-библиотека для создания и проверки JWT-токенов.

---

# 🔍 Что это за библиотека?

**JJWT** — это _io.jsonwebtoken_ — официальная библиотека от Okta для работы с JWT.

Она позволяет:

- генерировать JWT токены
    
- подписывать их секретом или RSA-ключами
    
- парсить и валидировать токены
    
- работать с claims (payload токена)
    

---

# 🧩 Разбор твоих импортов

### ✔ `io.jsonwebtoken.Claims`

- Объект, представляющий **claims** — данные внутри токена (payload).
    
- Например: id пользователя, роли, срок действия и др.
    

### ✔ `io.jsonwebtoken.Jwts`

- Основной класс-фабрика для:
    
    - создания токена
        
    - парсинга токена
        
    - проверки подписи токена
        

### ✔ `io.jsonwebtoken.SignatureAlgorithm`

- Перечисление доступных алгоритмов подписи:
    
    - HS256 (HMAC)
        
    - RS256 (RSA)
        
    - ES256 (ECDSA)
        
- Используется для выбора способа подписи токена.