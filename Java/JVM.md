## 🔹 1. JVM, JDK, JRE

- **JVM (Java Virtual Machine)**  
    Виртуальная машина, которая исполняет байткод. Она:
    
    - загружает `.class` файлы (байткод);
        
    - управляет памятью (heap, stack, metaspace);
        
    - исполняет код (через интерпретацию и JIT-компиляцию);
        
    - обеспечивает _платформенную независимость_ ("Write Once, Run Anywhere").
        
- **JRE (Java Runtime Environment)**  
    Минимальный набор для запуска Java-программ: JVM + стандартные библиотеки.  
    ➝ Нет компилятора. Используется только для запуска.
    
- **JDK (Java Development Kit)**  
    JRE + инструменты разработчика (javac, javadoc, debugger).  
    ➝ Нужен для разработки.
    

---

## 🔹 2. Как работает Garbage Collector (GC)

GC — это автоматическая очистка памяти от объектов, на которые нет ссылок.

### Основные этапы:

1. **Mark (Пометка)** — GC обходит граф объектов из "корней" (стек, статические переменные, активные потоки) и помечает "живые" объекты.
    
2. **Sweep (Очистка)** — убирает неиспользуемые объекты.
    
3. **Compact (Сжатие, не всегда)** — перемещает объекты, чтобы убрать фрагментацию памяти.
    

### Виды GC в HotSpot(**HotSpot = самая распространённая реализация JVM**, которая оптимизирует код «на лету» и делает Java-программы быстрее.):

- **Serial GC** — простой, для однопоточных приложений.
    
- **Parallel GC** — многопоточный, ориентирован на throughput.
    
- **G1 GC** — делит heap на регионы, оптимизирует паузы.
    
- **ZGC, Shenandoah** — низколатентные, работают почти без пауз.
    

📌 Пример "утечки памяти" (GC не освободит, если есть ссылки):

`import java.util.ArrayList; import java.util.List;  public class MemoryLeakDemo {     static List<String> cache = new ArrayList<>();      public static void main(String[] args) {         while (true) {             cache.add(new String("Data")); // ссылки остаются в списке         }     } }`

---

## 🔹 3. Metaspace и PermGen

- До Java 8:  
    **PermGen (Permanent Generation)** — отдельная область памяти для метаданных классов.  
    ➝ Часто были ошибки `OutOfMemoryError: PermGen space`.
    
- Начиная с Java 8:  
    **Metaspace** (заменил PermGen).
    
    - хранит метаданные классов в _native memory_, а не в heap;
        
    - автоматически расширяется (ограничен только RAM);
        
    - уменьшило количество OOM(**OutOfMemoryError**.) ошибок.
        

---

## 🔹 4. Как запускается Java-программа

1. Ты пишешь `.java` файл.
    
2. **javac** компилирует → `.class` (байткод).
    
3. JVM:
    
    - Загружает классы через **ClassLoader**;
        
    - Проверяет корректность байткода (Verifier);
        
    - Создаёт объекты в heap;
        
    - Управляет потоками;
        
    - Выполняет байткод через интерпретатор или JIT.
        

---

## 🔹 5. JIT-компилятор (Just-In-Time)

JVM сначала **интерпретирует** байткод (строка за строкой).  
Если метод часто вызывается → JIT компилирует его в **машинный код** и кеширует.

- ➝ Первые вызовы метода медленные (интерпретация).
    
- ➝ Дальше метод работает на скорости нативного кода(- **Байт-код** — это промежуточный код, который понимает JVM, но процессор "железно" его выполнять не умеет.
    
- **Нативный код** — это машинные инструкции, специфичные для архитектуры процессора (например, x86-64, ARM). То есть это те самые "единицы и нули", которые напрямую выполняются CPU.
    

Когда JIT-компилятор работает:

1. Он берёт байт-код (например, `iload_1`, `iadd`, `istore_2`)
    
2. Преобразует их в **машинные инструкции конкретного процессора** (например, `MOV`, `ADD`, `STORE` в x86-64).
    
3. В итоге программа начинает выполняться **напрямую процессором**, а не через интерпретацию JVM.
    

Поэтому нативный код зависит от:

- ОС (Windows, Linux, macOS)
    
- Архитектуры процессора (x86, x64, ARM).
    

Пример:

- Один и тот же `.class` файл может быть выполнен как на Windows (x86-64), так и на Android (ARM).
    
- JVM и JIT позаботятся о том, чтобы байт-код перевести в подходящий **нативный код** именно для этого окружения.).
    

📌 Пример:

`public class JitExample {     public static void main(String[] args) {         long start = System.nanoTime();         for (int i = 0; i < 1_000_000_000; i++) {             mathOperation(); // JIT оптимизирует этот вызов         }         System.out.println("Time: " + (System.nanoTime() - start));     }      private static int mathOperation() {         return 42 * 2;     } }`

👉 После прогрева JVM метод `mathOperation` будет оптимизирован JIT и станет почти такой же быстрый, как C-код.