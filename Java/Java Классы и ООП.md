
### 1. Инкапсуляция, наследование, полиморфизм

- **Инкапсуляция**  
    Скрытие внутреннего состояния объекта и ограничение доступа к нему — обычно через `private` поля и `public` методы (`get`, `set`). Обеспечивает целостность данных и уменьшает взаимозависимость компонентов.  
    [Википедия](https://en.wikipedia.org/wiki/Encapsulation_%28computer_programming%29?utm_source=chatgpt.com)
    
- **Наследование**  
    Механизм, позволяющий классу наследовать свойства и методы другого. Позволяет повторно использовать код и устанавливать отношения "является-экземпляром" (`extends`).  
    [GeeksforGeeks](https://www.geeksforgeeks.org/java/object-oriented-programming-oops-concept-in-java/?utm_source=chatgpt.com)
    
- **Полиморфизм**  
    Способность объекта вести себя по-разному в зависимости от того, какой тип у ссылки. Часто используется через переопределение методов (runtime polymorphism) и интерфейсы.  
    [GeeksforGeeks](https://www.geeksforgeeks.org/java/object-oriented-programming-oops-concept-in-java/?utm_source=chatgpt.com)[nerd.vision](https://www.nerd.vision/post/polymorphism-encapsulation-data-abstraction-and-inheritance-in-object-oriented-programming?utm_source=chatgpt.com)
    

---

### 2. Наследование vs Композиция — когда что использовать

- **Наследование** — подходит, когда есть чёткая иерархия и "является" (“is-a”) отношение между классами.
    
- **Композиция** — предпочтительна, когда класс использует другой класс через его экземпляр (“has-a”), что снижает связанность и повышает гибкость.  
    [Википедия](https://en.wikipedia.org/wiki/Composition_over_inheritance?utm_source=chatgpt.com)
    

Композицию часто считают более устойчивым и гибким подходом по сравнению с наследованием.

---

### 3. Паттерны: Builder, Singleton, Factory

**Порождающие паттерны** позволяют гибко создавать объекты:

- **Singleton (Одиночка)** — гарантирует наличие лишь одного экземпляра класса и глобальную точку доступа к нему.  
    [Википедия](https://ru.wikipedia.org/wiki/%D0%9E%D0%B4%D0%B8%D0%BD%D0%BE%D1%87%D0%BA%D0%B0_%28%D1%88%D0%B0%D0%B1%D0%BB%D0%BE%D0%BD_%D0%BF%D1%80%D0%BE%D0%B5%D0%BA%D1%82%D0%B8%D1%80%D0%BE%D0%B2%D0%B0%D0%BD%D0%B8%D1%8F%29?utm_source=chatgpt.com)
    
- **Builder (Строитель)** — предоставляет способ поэтапного и гибкого построения сложных объектов, отделяя конфигурацию от представления.  
    [Википедия](https://ru.wikipedia.org/wiki/%D0%A1%D1%82%D1%80%D0%BE%D0%B8%D1%82%D0%B5%D0%BB%D1%8C_%28%D1%88%D0%B0%D0%B1%D0%BB%D0%BE%D0%BD_%D0%BF%D1%80%D0%BE%D0%B5%D0%BA%D1%82%D0%B8%D1%80%D0%BE%D0%B2%D0%B0%D0%BD%D0%B8%D1%8F%29?utm_source=chatgpt.com)
    
- **Factory (Фабричный метод)** — делегирует логику создания объектов подклассам, что позволяет создавать разные реализации через единый интерфейс.  
    [Википедия](https://ru.wikipedia.org/wiki/%D0%A4%D0%B0%D0%B1%D1%80%D0%B8%D1%87%D0%BD%D1%8B%D0%B9_%D0%BC%D0%B5%D1%82%D0%BE%D0%B4_%28%D1%88%D0%B0%D0%B1%D0%BB%D0%BE%D0%BD_%D0%BF%D1%80%D0%BE%D0%B5%D0%BA%D1%82%D0%B8%D1%80%D0%BE%D0%B2%D0%B0%D0%BD%D0%B8%D1%8F%29?utm_source=chatgpt.com)
    

Skillbox приводит простые примеры и объяснение применения этих паттернов.  
[skillbox.ru](https://skillbox.ru/media/code/osnovnye-patterny-proektirovaniya-na-java/?utm_source=chatgpt.com)

---

### 4. Внутренние классы, static-классы, анонимные классы

Java поддерживает несколько видов классов внутри классов:
## **1. Нестатические внутренние классы (Inner Class)**

📌 **Что это**

- Это класс, объявленный **внутри другого класса** без модификатора `static`.
    
- Привязан к конкретному объекту внешнего класса.
    
- Может обращаться к **всем полям и методам** внешнего класса, включая `private`.
    

📌 **Пример:**

public class Outer {
    private String message = "Hello";

    public class Inner {
        public void printMessage() {
            System.out.println(message); // доступ к private
        }
    }

    public static void main(String[] args) {
        Outer outer = new Outer();
        Outer.Inner inner = outer.new Inner(); // создаём через экземпляр outer
        inner.printMessage(); // Hello
    }
}


📌 **Особенности:**

- Чтобы создать inner-класс, нужен объект внешнего класса (`outer.new Inner()`).
    
- До Java 16 нельзя было объявлять `static`-переменные (кроме `final static` констант).
    
- Часто используется, когда логически класс не может существовать без внешнего.
    

---

## **2. Статические вложенные классы (Static Nested Class)**

📌 **Что это**

- Вложенный класс с модификатором `static`.
    
- **Не привязан** к экземпляру внешнего класса.
    
- Не имеет доступа к нестатическим полям внешнего класса напрямую.
    

📌 **Пример:**

public class Outer {
    private static String staticMessage = "Hello from static";
    private String nonStaticMessage = "Hello from non-static";

    public static class Nested {
        public void printMessage() {
            System.out.println(staticMessage); // доступно
            // System.out.println(nonStaticMessage); // ошибка
        }
    }

    public static void main(String[] args) {
        Nested nested = new Nested(); // создаём без объекта Outer
        nested.printMessage();
    }
}


📌 **Особенности:**

- Можно создавать без объекта внешнего класса.
    
- Полезно для вспомогательных классов, которые логически связаны с внешним.
    

---

## **3. Локальные классы (Local Class)**

📌 **Что это**

- Класс, объявленный внутри метода, конструктора или блока и доступный только внутри этой области видимости.
    
- Может обращаться к **final** или **effectively final** (не меняющимся после присвоения) локальным переменным метода.
    

📌 **Пример:**

public class Outer {
    public void doSomething() {
        String localVar = "Local Variable";

        class LocalClass {
            void print() {
                System.out.println(localVar);
            }
        }

        LocalClass lc = new LocalClass();
        lc.print();
    }

    public static void main(String[] args) {
        new Outer().doSomething();
    }
}


📌 **Особенности:**

- Используются редко, чаще — как вспомогательные классы внутри алгоритмов.
    
- Переменные должны быть `effectively final`.
    

---

## **4. Анонимные классы (Anonymous Class)**

📌 **Что это**

- Класс **без имени**, создаваемый для одноразового использования.
    
- Может расширять существующий класс или реализовывать интерфейс.
    
- Часто используется для **быстрых коллбэков** или **обработчиков событий**.
    

📌 **Пример:**

public class Demo {
    interface Greeting {
        void sayHello();
    }

    public static void main(String[] args) {
        Greeting g = new Greeting() {
            @Override
            public void sayHello() {
                System.out.println("Hello from anonymous class!");
            }
        };

        g.sayHello();
    }
}


📌 **Особенности:**

- Не поддерживают `static`-члены (до Java 16).
    
- Компилятор создаёт `.class` файл с именем вроде `Demo$1.class`.
    
- Отлично подходят для одноразовых реализаций интерфейсов (особенно до появления **lambda** в Java 8).
    

---

## **Сравнение видов внутренних классов**

|Вид класса|Привязан к объекту внешнего|Доступ к private полям|Можно `static` поля|Пример использования|
|---|---|---|---|---|
|Inner|✅|✅|❌ (кроме констант)|Логически неразрывная связь с внешним классом|
|Static Nested|❌|❌ (только через объект)|✅|Вспомогательные классы|
|Local|✅ (через метод)|✅ (к final переменным)|❌|Временные классы внутри алгоритмов|
|Anonymous|✅|✅ (к final переменным)|❌|Одноразовые реализации интерфейсов/классов|
---


## Таблица быстрого обзора

| Тема                       | Суть                                                      |
| -------------------------- | --------------------------------------------------------- |
| Инкапсуляция               | Скрытие данных, доступ через методы                       |
| Наследование               | Расширение существующих классов                           |
| Полиморфизм                | Поведение зависит от типа ссылки                          |
| Наследование vs Композиция | "is-a" vs "has-a"; композиция гибче                       |
| Singleton                  | Один экземпляр, глобальный доступ                         |
| Builder                    | Пошаговая сборка сложных объектов                         |
| Factory                    | Делегирование создания объектов                           |
| Нестатические inner-классы | Привязаны к экземплярам внешнего класса                   |
| Статические nested-классы  | Независимы от внешнего класса, могут быть static          |
| Локальные классы           | Внутри методов, видны только там                          |
| Анонимные классы           | Без имени, расширяют классы/интерфейсы прямо при создании |

## **5. Дополнительно (для собеседований)**

### **5.1. Разница между `abstract class` и `interface`**

|Критерий|Abstract Class|Interface|
|---|---|---|
|Множественное наследование|❌ Только одно наследование|✅ Можно реализовать несколько интерфейсов|
|Поля|✅ Может иметь поля (в т.ч. с разными модификаторами)|⚠ Только `public static final` (константы)|
|Методы|✅ Может содержать как абстрактные, так и с реализацией|✅ Методы с реализацией (default, static) появились с Java 8|
|Конструктор|✅ Есть|❌ Нет|
|Когда использовать|Когда нужно общее состояние или базовая логика|Когда нужно только API без реализации|

Пример:

`abstract class Shape {     
	abstract double area();     
	void print() { 
		System.out.println("Shape"); }
 }  
interface Drawable {     void draw(); }`

---

### **5.2. `Object` и его ключевые методы**

Класс `java.lang.Object` — родитель всех классов в Java.

- **`equals(Object obj)`** — сравнение объектов по логике, определённой программистом.
    
- **`hashCode()`** — возвращает целое число (хэш), связанное с объектом, важно для работы в коллекциях (`HashMap`, `HashSet`).
    
- **`toString()`** — строковое представление объекта.
    
- **`clone()`** — создание копии объекта (поверхностное копирование).
    
- **`finalize()`** — вызывается перед удалением объекта сборщиком мусора (устарел).
    

---

### **5.3. Модификаторы доступа**

|Модификатор|Видимость|Где используется|
|---|---|---|
|`private`|Только в текущем классе|Инкапсуляция данных|
|`default` (package-private)|В пределах пакета|Для сервисов внутри модуля|
|`protected`|В пакете + в наследниках|Для расширяемости|
|`public`|Доступ отовсюду|Для API|

---

### **5.4. Иммутабельные классы**

Иммутабельный (immutable) класс — объект, состояние которого нельзя изменить после создания.

**Как создать:**

1. Сделать поля `final`.
    
2. Не давать сеттеров.
    
3. Возвращать копии изменяемых объектов в геттерах.
    
4. Сделать класс `final` (чтобы нельзя было наследовать).
    

Пример:

`public final class Person {     
	private final String name;     
	private final int age;     
	public Person(String name, int age) {         
		this.name = name;         this.age = age;     }    
	 public String getName() { return name; }    
	 public int getAge() { return age; } }`

**Зачем нужны:** Потокобезопасность, удобство в кэшировании, предсказуемость.

---

## **5.5. Принципы SOLID**

**SOLID** — это набор из пяти принципов проектирования, предложенных Робертом Мартином (Uncle Bob), которые помогают писать гибкий, поддерживаемый и легко расширяемый код.

---

### **1. S — Single Responsibility Principle (SRP)**

**Принцип единственной ответственности**

> Класс должен иметь **только одну причину для изменения**.

**Что значит:**  
Каждый класс или модуль должен выполнять **только одну задачу**. Если класс отвечает за несколько вещей, он становится сложным для понимания и поддержки.

**Пример нарушения:**

class UserService {
    public void createUser(User user) { /* ... */ }
    public void sendEmail(User user) { /* ... */ } // Лишняя ответственность
}


Здесь `UserService` отвечает **и за создание пользователя**, и за **отправку писем** — две разные ответственности.

**Правильный подход:**

class UserService {
    public void createUser(User user) { /* ... */ }
}

class EmailService {
    public void sendEmail(User user) { /* ... */ }
}


**Почему важно:**

- Проще тестировать и изменять код.
    
- Изменение в одной области не ломает другую.
    

---

### **2. O — Open/Closed Principle (OCP)**

**Принцип открытости/закрытости**

> Программные сущности должны быть **открыты для расширения, но закрыты для модификации**.

**Что значит:**  
Мы должны иметь возможность **добавлять новый функционал** без изменения уже существующего кода.

**Пример нарушения:**

class PaymentProcessor {
    public void process(String type) {
        if (type.equals("CreditCard")) { /* ... */ }
        else if (type.equals("PayPal")) { /* ... */ }
    }
}


Если добавить новый тип оплаты, придётся **лезть в код класса**.

**Правильный подход (через полиморфизм):**

interface PaymentMethod {
    void pay();
}

class CreditCardPayment implements PaymentMethod {
    public void pay() { /* ... */ }
}

class PayPalPayment implements PaymentMethod {
    public void pay() { /* ... */ }
}

class PaymentProcessor {
    public void process(PaymentMethod method) {
        method.pay();
    }
}


Теперь мы можем **добавить новый способ оплаты**, просто реализовав `PaymentMethod`.

---

### **3. L — Liskov Substitution Principle (LSP)**

**Принцип подстановки Лисков**

> Объекты дочерних классов должны **заменять** объекты родительских без изменения корректности программы.

**Что значит:**  
Подкласс не должен нарушать поведение, ожидаемое от родительского класса.

**Пример нарушения:**

class Bird {
    public void fly() { System.out.println("Flying"); }
}

class Ostrich extends Bird {
    @Override
    public void fly() {
        throw new UnsupportedOperationException("Страусы не летают");
    }
}


Используя `Ostrich` там, где ожидается `Bird`, мы ломаем логику.

**Правильный подход:**

interface Bird { }

interface FlyableBird extends Bird {
    void fly();
}

class Sparrow implements FlyableBird {
    public void fly() { System.out.println("Flying"); }
}

class Ostrich implements Bird { }


---

### **4. I — Interface Segregation Principle (ISP)**

**Принцип разделения интерфейсов**

> Клиенты не должны зависеть от методов, которые они **не используют**.

**Что значит:**  
Лучше иметь много маленьких интерфейсов, чем один огромный.

**Пример нарушения:**

interface Worker {
    void work();
    void eat();
}

class Robot implements Worker {
    public void work() { /* ... */ }
    public void eat() { throw new UnsupportedOperationException(); }
}


Робот не ест, но вынужден реализовывать `eat()`.

**Правильный подход:**

interface Workable {
    void work();
}

interface Eatable {
    void eat();
}

class Robot implements Workable {
    public void work() { /* ... */ }
}


---

### **5. D — Dependency Inversion Principle (DIP)**

**Принцип инверсии зависимостей**

> Модули верхнего уровня не должны зависеть от модулей нижнего уровня — оба должны зависеть от **абстракций**.

**Что значит:**  
Мы должны зависеть от **интерфейсов**, а не от конкретных реализаций.

**Пример нарушения:**

class FileLogger {
    public void log(String message) { /* ... */ }
}

class UserService {
    private FileLogger logger = new FileLogger();
}


`UserService` жёстко зависит от `FileLogger`.

**Правильный подход:**

interface Logger {
    void log(String message);
}

class FileLogger implements Logger {
    public void log(String message) { /* ... */ }
}

class ConsoleLogger implements Logger {
    public void log(String message) { /* ... */ }
}

class UserService {
    private final Logger logger;
    public UserService(Logger logger) {
        this.logger = logger;
    }
}


Теперь можно подставить любой `Logger` без изменения `UserService`.

---

💡 **Итоговая польза SOLID:**

- **SRP** — проще поддерживать и изменять.
    
- **OCP** — легче добавлять новые функции.
    
- **LSP** — код предсказуем.
    
- **ISP** — меньше ненужных зависимостей.
    
- **DIP** — легко заменять реализации.
    

---

### **5.6. Lombok и подводные камни**

Аннотации:

- `@Data` — генерирует геттеры, сеттеры, `equals`, `hashCode`, `toString`.
    
- `@Builder` — шаблон "Строитель".
    
- `@Value` — иммутабельный класс.
    

**Подводные камни:**

- Может ухудшать читаемость кода для новых разработчиков.
    
- Может ломать контракт `equals`/`hashCode`, если неаккуратно применять.
    
- Требует плагина в IDE.
    
- Усложняет отладку (методы генерируются на этапе компиляции).
    

---

### **5.7. Composition over Inheritance**

- **Наследование** (`extends`) связывает классы жёстко — изменения в родителе могут сломать потомков.
    
- **Композиция** — включение объекта как поля внутри класса, делегирование вызовов.
    

**Пример:**

`// Наследование class ElectricCar extends Car {}  // Композиция class ElectricCar {     private final Car car;     ElectricCar(Car car) { this.car = car; } }`

**Когда применять:** В большинстве случаев предпочтительнее композиция, т.к. она гибче и безопаснее.

### 6. Работа со строками в Java

**String, StringBuilder, StringBuffer, String Pool** — важная тема для собеседований, так как напрямую связана с памятью, производительностью и безопасностью потоков.

---

#### **String**

- Класс **String** в Java **иммутабельный** — после создания изменить его содержимое невозможно.
    
- Любая операция "изменения" строки (например, `concat`, `replace`, `substring`) создаёт **новый объект**.
    
- Пример:
    
    `String a = "Hello"; a.concat(" World"); // создаётся новый объект "Hello World", но 'a' остаётся "Hello"`
    

---

#### **StringBuilder**

- **Неиммутабельный** класс для работы со строками.
    
- Оптимизирован для **однопоточных** сценариев.
    
- Быстрее, чем `StringBuffer`, так как не имеет синхронизации.
    
- Пример:
    
    `StringBuilder sb = new StringBuilder("Hello"); sb.append(" World"); System.out.println(sb); // Hello World`
    

---

#### **StringBuffer**

- **Неиммутабельный**, как `StringBuilder`, но **потокобезопасный** (все методы синхронизированы).
    
- Из-за синхронизации **медленнее**, чем `StringBuilder`.
    
- Используется, если строку модифицируют **из нескольких потоков**.
    

---

#### **Сравнение производительности**

- `StringBuilder` > `StringBuffer` > `String` (при частых изменениях строк).
    
- Если строка **не меняется** — использовать `String` (экономия памяти за счёт String Pool).
    
- Если строка часто меняется в **одном потоке** — `StringBuilder`.
    
- Если строка часто меняется в **многопоточной среде** — `StringBuffer`.
    

---

#### **String Pool**

- В Java **String Pool** (иногда называют _intern pool_) — это специальная область памяти в **Heap**, где хранятся **уникальные** строковые литералы.

### 🔹 Что хранится в String Pool

- Только **неизменяемые** (`immutable`) объекты класса `String`.
    
- Только **уникальные** значения — если создаётся строка с тем же содержимым, то будет возвращена ссылка на уже существующую строку из пула.
    

---

### 🔹 Пример работы

`String a = "Hello"; String b = "Hello";  System.out.println(a == b); // true — ссылки одинаковые`

**Что происходит:**

1. `"Hello"` — литерал → JVM проверяет в String Pool, есть ли уже такая строка.
    
2. Если есть — возвращается ссылка.
    
3. Если нет — создаётся новый объект в пуле.
    

---

### 🔹 Почему это работает

- Класс `String` в Java **immutable** → содержимое строки никогда не изменится после создания.
    
- JVM может безопасно переиспользовать один и тот же объект без риска неожиданного изменения.
    

---

### 🔹 `new String(...)` и String Pool

`String a = "Java"; String b = new String("Java");  System.out.println(a == b); // false`

- `"Java"` — объект в String Pool.
    
- `new String("Java")` — **новый объект** в Heap, даже если в пуле уже есть такая строка.
    

---

### 🔹 `intern()`

Метод `intern()` заставляет JVM использовать объект из пула:

`String s1 = new String("Hello"); String s2 = s1.intern();  System.out.println(s1 == s2); // false System.out.println(s2 == "Hello"); // true`

---

### 🔹 Память

- String Pool создавался в **PermGen** (до Java 7) → ограниченная память.
    
- С Java 7 String Pool перенесён в **Heap** → можно хранить больше строк.
    
- Каждая уникальная строка в пуле хранится до тех пор, пока на неё есть ссылка (или до очистки GC, если она стала неиспользуемой).
    

---

## **2. StringBuilder**

**`StringBuilder`** — изменяемый (`mutable`) класс для работы со строками.

---

### 🔹 Почему нужен StringBuilder

В Java строки **immutable**:

`String s = "A"; s += "B"; // создаётся НОВЫЙ объект "AB"`

- Каждый раз при изменении строки создаётся новый объект → затраты памяти и времени.
    
- Если много операций конкатенации — это дорого.
    

---

### 🔹 Как работает StringBuilder

В основе лежит **массив `char[]`**, который можно изменять без создания нового объекта:

`StringBuilder sb = new StringBuilder("Hello"); sb.append(" World");`

**Под капотом:**

1. Создаётся `char[]` с запасом по длине (обычно на 16 символов больше).
    
2. Метод `append()` копирует новые символы в массив.
    
3. Если массив переполняется → создаётся новый, в 2 раза больше старого, и данные копируются.
    

---

### 🔹 Производительность

`// Медленно String s = ""; for (int i = 0; i < 10000; i++) {     s += i; // каждый раз создаётся новый объект }  // Быстро StringBuilder sb = new StringBuilder(); for (int i = 0; i < 10000; i++) {     sb.append(i); // всё в одном char[] } String result = sb.toString();`

- `StringBuilder` быстрее в **десятки раз** при больших количествах изменений.
    

---

### 🔹 Потокобезопасность

- `StringBuilder` — **не потокобезопасный**.
    
- Если нужно работать в многопоточной среде → использовать `StringBuffer` (он синхронизирован, но медленнее).
    

---

### 🔹 Когда использовать

- Много операций конкатенации — **`StringBuilder`**.
    
- Небольшое количество операций — можно оставить `String` (компилятор сам оптимизирует через `StringBuilder` в циклах).
    
- Многопоточность — `StringBuffer`.
    

---

## 📌 Итоговая схема

| Особенность            | String (Pool)         | StringBuilder          |
| ---------------------- | --------------------- | ---------------------- |
| **Изменяемость**       | Нет                   | Да                     |
| **Хранение**           | Heap + Pool           | Heap                   |
| **Оптимизация памяти** | Да (пул)              | Да (char[])            |
| **Производительность** | Низкая при изменениях | Высокая при изменениях |
| **Потокобезопасность** | Да (immutable)        | Нет                    |
![[Pasted image 20250815005936.png]]