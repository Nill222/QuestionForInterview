# **Коллекции в Java: List, Set, Map и их реализация**

## **1. Разница между List, Set, Map**

### **List** — упорядоченный список элементов

- **Допускает дубликаты**
    
- Элементы имеют **индекс** (можно получить по номеру)
    
- **Сохраняет порядок добавления**
	

**Примеры:**

- `ArrayList` — быстрый доступ по индексу, но медленные вставки в середину.
    
- `LinkedList` — удобен для частых вставок/удалений, но медленный доступ по индексу.
    

`List<String> list = new ArrayList<>(); list.add("A"); list.add("B"); list.add("A"); // дубликат допустим`

	Разница между **обычным массивом (`T[]`)** и **`ArrayList<T>`** в Java — как между коробкой с фиксированным числом ячеек и «резиновым» органайзером, который сам растёт, когда нужно.

Давай разберёмся по пунктам:

---

## **1. Размер**

- **Обычный массив**
    
    - Размер **фиксируется при создании**.
        
    - Увеличить или уменьшить его нельзя без создания нового массива.
        
    
    `String[] arr = new String[3]; // навсегда на 3 элемента`
    
- **ArrayList**
    
    - Размер меняется автоматически.
        
    - При добавлении элементов он создаёт внутри **новый массив большего размера** и копирует туда старые данные.
        
    
    `ArrayList<String> list = new ArrayList<>(); list.add("A"); // можно добавлять бесконечно (пока хватает памяти)`
    

---

## **2. Гибкость**

- **Массив** — структура данных, работаешь напрямую с индексами.
    
- **ArrayList** — обёртка над массивом с удобными методами:
    
    - `add()`
        
    - `remove()`
        
    - `contains()`
        
    - `size()`
        
    - `indexOf()` и т.д.
        

`// массив arr[0] = "Hello";  // ArrayList list.add("Hello"); list.remove("Hello");`

---

## **3. Типизация**

- **Массив** — может хранить как примитивы (`int[]`, `double[]`), так и объекты.
    
- **ArrayList** — работает **только с объектами**.  
    Если нужен список чисел, используются обёртки (`Integer`, `Double`), а Java сама делает **autoboxing**:
    
    `ArrayList<Integer> numbers = new ArrayList<>(); numbers.add(10); // автоконвертация int → Integer`
    

---

## **4. Производительность**

- **Массив** — быстрее, потому что нет дополнительных проверок и расширений.
    
- **ArrayList** — чуть медленнее при вставках/удалениях из середины, так как элементы сдвигаются.
    

---

## **5. Под капотом**

- `ArrayList` хранит данные **внутри обычного массива** (`Object[] elementData`).
    
- При нехватке места создаёт новый массив и копирует туда старые элементы (увеличивает примерно на 50%).
    

---

## **6. Когда использовать**

- **Массив** — когда:
    
    - Нужно фиксированное количество элементов.
        
    - Важна максимальная производительность.
        
- **ArrayList** — когда:
    
    - Размер списка меняется.
        
    - Нужны удобные методы управления коллекцией.
        

---

### 📌 Мини-сравнение

|Свойство|Массив (`T[]`)|ArrayList<T>|
|---|---|---|
|Размер|Фиксированный|Автоматически изменяемый|
|Примитивы|Поддерживает|Только объекты|
|Методы|Нет|Есть (`add`, `remove`, …)|
|Скорость|Быстрее|Немного медленнее|
|Под капотом|-|Обычный массив|
---

### **Set** — множество

- **Не допускает дубликаты**
    
- Нет индексов
    
- Порядок зависит от реализации:
    
    - `HashSet` — порядок не гарантирован
        
    - `LinkedHashSet` — сохраняет порядок добавления
        
    - `TreeSet` — сортирует по `Comparable` или `Comparator`
        

`Set<String> set = new HashSet<>(); set.add("A"); set.add("B"); set.add("A"); // не добавится — уже есть`

💡 **Под капотом `HashSet`**  
Реализован через `HashMap`, где ключ — элемент множества, а значение — константный объект-заглушка.

`public class HashSet<E> implements Set<E> {     
private transient HashMap<E, Object> map;     
private static final Object PRESENT = new Object(); }`

---

### **Map** — отображение ключ → значение

- Каждый ключ уникален
    
- Значение может повторяться
    
- Порядок зависит от реализации:
    
    - `HashMap` — порядок не гарантирован
        
    - `LinkedHashMap` — порядок вставки
        
    - `TreeMap` — сортировка по ключу
        

Map<String, Integer> map = new HashMap<>();
map.put("apple", 1);
map.put("banana", 2);


---

## **2. Алгоритм работы `HashSet.add()`**

1. **Вычисляется `hashCode()`** объекта.
    
2. **Применяется хэш-функция** для равномерного распределения бит:
    
    static final int hash(Object key) {
    int h;
    return (key == null) ? 0 : (h = key.hashCode()) ^ (h >>> 16);
}

    
3. **Выбирается корзина (bucket)**:
    
    `index = (n - 1) & hash; // n — длина массива (степень 2)`
    
4. **Проверка коллизий**:
    
    - Если бакет пуст — кладём элемент.
        
    - Если не пуст — сравнение по hash → equals().
        
    - При совпадении — элемент не добавляется.
        
5. **Балансировка дерева** (Java 8+):
    
    - Если в корзине > 8 элементов → LinkedList превращается в красно-чёрное дерево (O(log n)).
        

📌 Уникальность определяется комбинацией **`hashCode` + `equals`**.

---

## **3. Рост `HashSet`**

- Начальная ёмкость: 16
    
- Load factor: 0.75
    
- При превышении `capacity * loadFactor` → увеличение массива в 2 раза и **rehash**.
    

---

## **4. Важные нюансы `HashSet`**

- Допускает **один null**
    
- Порядок элементов **не гарантирован**
    
- Средняя сложность вставки/поиска — **O(1)**, но при коллизиях может быть **O(n)**.
    

---

## **5. HashMap: устройство и работа**

### **Структура**

static class Node<K, V> implements Map.Entry<K, V> {
    final int hash;
    final K key;
    V value;
    Node<K, V> next;
}


- `table[]` — массив бакетов.
    
- Каждый бакет хранит либо `null`, либо цепочку (LinkedList) или дерево (TreeNode).
    

---

### **put(key, value)**

1. Вычисляется hash ключа.
    
2. Находится индекс корзины.
    
3. Если пусто — создаётся новый Node.
    
4. Если есть — проверка hash и equals:
    
    - Совпадает → перезапись.
        
    - Не совпадает → добавление в цепочку/дерево.
        
5. При цепочке > 8 и массиве > 64 — преобразование в дерево.
    

---

### **get(key)**

1. Вычисляется hash.
    
2. Находится корзина.
    
3. Если пусто → `null`.
    
4. Если есть — поиск через equals (в дереве — бинарный поиск).
    

---

### **Коллизии**

- Разные ключи → один индекс корзины.
    
- Java 7: LinkedList
    
- Java 8+: LinkedList → TreeNode при цепочке > 8.
    

---

### **Ресайз**

- Условие: `size > capacity * loadFactor`
    
- Новый массив в 2 раза больше.
    
- Java 8: перераспределение без пересчёта hash.
    

---

### **Null**

- Один ключ `null` (корзина 0).
    
- Значений `null` — любое количество.
    

---

## **6. Когда использовать HashMap, LinkedHashMap, TreeMap**

| Коллекция     | Сложность | Особенности                                    |
| ------------- | --------- | ---------------------------------------------- |
| HashMap       | O(1)      | Быстрая работа, порядок ключей не гарантирован |
| LinkedHashMap | O(1)      | Сохраняет порядок вставки                      |
| TreeMap       | O(log n)  | Сортировка по ключам, удобно для диапазонов    |

---

## **7. Зачем переопределять equals() и hashCode()**

- **equals()** — сравнение по содержимому.
    
- **hashCode()** — быстрый доступ в хэш-структурах.
    

**Контракт:**

1. Если equals() → true, hashCode() должен совпадать.
    
2. Если equals() → false, hashCode() может совпадать (коллизия).
    

---

## **8. ConcurrentHashMap и CopyOnWriteArrayList**

### **ConcurrentHashMap**

- Потокобезопасный HashMap.
    
- Лочит только сегменты, а не всю карту.
    
- Быстрее, чем Hashtable.
    

### **CopyOnWriteArrayList**

- Потокобезопасный List.
    
- Каждая модификация → копия массива.
    
- Идеален при частом чтении и редких изменениях.




 `equals()` и `hashCode()` в Java **жёстко связаны контрактом**, и если его нарушить, коллекции типа `HashMap` или `HashSet` начнут вести себя странно.

Разберём **по шагам** и с примерами.

---

## **1. Назначение**

- **`equals()`** — определяет, считаем ли мы два объекта "одинаковыми" по содержимому.
    
- **`hashCode()`** — возвращает число (int), используемое при хранении объекта в хэш-структурах (`HashMap`, `HashSet` и т. д.) для **быстрого поиска корзины**.
    

---

## **2. Контракт `equals()` и `hashCode()`**

Java требует:

1. **Если `equals()` вернул `true`**, оба объекта должны иметь **одинаковый** `hashCode()`.
    
2. **Если `equals()` вернул `false`**, `hashCode()` может совпадать (это называется _коллизия_).
    
3. **`hashCode()` должен быть стабильным**: если объект не меняется, его `hashCode()` всегда одинаков.
    

---

## **3. Почему так важно**

Хэш-коллекции (`HashSet`, `HashMap`) работают в два этапа:

1. Сначала ищут **корзину** по `hashCode()`.
    
2. Потом внутри корзины сравнивают объекты через `equals()`.
    

Если ты переопределил **`equals()`**, но забыл про **`hashCode()`**, коллекция:

- Может положить **дубликаты** там, где не должно.
    
- Может "потерять" элемент — ты добавил его, а `contains()` вернёт `false`.
    

---

## **4. Пример ошибки**

		![[Pasted image 20250816014137.png]]

📌 Здесь два `Person` равны по `equals()`, но имеют разные `hashCode()` (из `Object` по умолчанию), поэтому попадают в разные корзины.

---

## **5. Правильная реализация**

`@Override public int hashCode() {     
return Objects.hash(name, age); }`

Теперь:

`System.out.println(set.contains(new Person("Tom", 20))); // ✅ true`

---

## **6. Как взаимодействует в `HashMap`**

При `put(key, value)`:

1. Берётся `hashCode()` ключа.
    
2. Определяется корзина.
    
3. В корзине ищется **ключ с таким же hash** и **equals() == true**.
    
4. Если нашли — перезаписываем value.
    
5. Если нет — добавляем новый Node.
    

---

## **7. Правила написания**

- Всегда переопределяй **оба** метода вместе.
    
- Поля, участвующие в `equals()`, должны участвовать и в `hashCode()`.
    
- Не используй поля, которые часто меняются (иначе объект «потеряется» в коллекции).
    

---

## **8. Важные нюансы**

- **Коллизии неизбежны** — два разных объекта могут иметь один и тот же hashCode.
    
- Хорошая хэш-функция уменьшает количество коллизий.
    
- Если объект используется как ключ в `HashMap`, **нельзя менять поля, участвующие в hashCode** после добавления в карту.
    

---

## **9. Визуальная логика**

HashSet.add(obj):
  1) hash = obj.hashCode()
  2) bucket = table[hash % size]
  3) for each element in bucket:
         if hash == element.hashCode() && obj.equals(element):
             не добавляем
  4) иначе — добавляем


Коллизии происходят, когда **два разных объекта имеют одинаковое значение `hashCode()`** (или одинаковый хеш, если мы говорим в терминах хэш-таблиц).

Это явление связано с тем, что **хэш — это сжатое представление данных**, и количество возможных значений хеша ограничено. Например:

- `int hashCode()` в Java имеет диапазон значений от `-2,147,483,648` до `2,147,483,647` (около 4,29 млрд значений).
    
- А объектов и их возможных комбинаций данных — гораздо больше.
    

В результате **разные объекты могут давать одинаковый хеш** — это и называется _коллизия_.

---

## 🔹 Когда коллизии возникают на практике

1. **Разные данные, но одинаковый результат hashCode()**
    
    String s1 = "FB";
	String s2 = "Ea";

	System.out.println(s1.hashCode()); // 2236
	System.out.println(s2.hashCode()); // 2236
	System.out.println(s1.equals(s2)); // false

    
    Здесь `hashCode` совпал случайно, но `equals` показывает, что объекты разные.
    

---

2. **Неправильная реализация `hashCode()`**  
    Например, если написать:
    
    @Override
	public int hashCode() {
    return 1; // Все объекты будут иметь одинаковый хэш
	}

    
    — это приведёт к максимальному числу коллизий.
    

---

3. **Ограниченность диапазона int**  
    Даже хороший алгоритм не гарантирует уникальность из-за ограниченного диапазона значений — рано или поздно коллизия произойдёт.
    

---

4. **Использование только части данных объекта**  
    Если в `hashCode()` учитывать не все поля, то разные объекты могут давать одинаковый хеш:
    
    class Person {
    String name;
    int age;

    @Override
    public int hashCode() {
        return age; // имя не учитывается
    }
	}

    
    Здесь все люди одного возраста будут иметь одинаковый хеш.
    

---

## 🔹 Что происходит при коллизии в HashMap / HashSet

- **До Java 8** — элементы с одинаковым хешем хранятся в **связанном списке** в бакете.
    
- **С Java 8** — при большом числе коллизий (более 8 элементов в бакете) список преобразуется в **сбалансированное дерево (red-black tree)**, что ускоряет поиск с O(n) до O(log n).
    

---

## 🔹 Как избежать проблем

- Всегда переопределяйте `equals` и `hashCode` вместе.
    
- Учитывайте в `hashCode()` все поля, участвующие в `equals`.
    
- Используйте хорошие алгоритмы хеширования (`Objects.hash(...)` или `HashCodeBuilder` из Apache Commons).